# Planning loop

This directory holds **STATE**, **TASK-REGISTRY**, **ROADMAP**, phase **PLAN**/**SUMMARY** files, and **templates**.

Bootstrap created `phases/01-greenfield/` with `01-01-PLAN.xml`. Edit **REQUIREMENTS.xml** and that plan, then run your planning CLI `snapshot`.

