---
title: "RichEPub Format Spec (Archived)"
description: "An archived note describing the earlier `.repub` container experiment and its manifest shape."
date: "2025-03-11"
tags: ["RichEPub", "Format", "Specification"]
links:
  - label: "Books Planning Docs"
    href: "/docs/books/planning-docs"
    description: "Current books direction after the `.repub` archive."
  - label: "Editor Docs"
    href: "/docs/editor"
    description: "Where the remaining tooling questions live."
searchKeywords: ["repub spec", "zip container", "archive format note", "builder versioning"]
---

## Downloads & Resources

- [Books Planning Docs](/docs/books/planning-docs) for the live books pipeline.
- [Editor Docs](/docs/editor) for the current authoring-tool discussion.

## Introduction

RichEPub (`.repub`) is a **ZIP-based container** with a fixed layout and a root manifest. This post is kept as an archived note from that experiment, not as the active recommendation for the books pipeline.

## Tradeoffs

A fixed container format makes packaging rules explicit, but it also forces the reader, builder, and documentation to move in lockstep. That coordination cost is exactly why the project stepped back toward EPUB3 for the current path.

## What Was Done

This note documents the old container shape, manifest fields, and versioning expectations so the archive remains referenceable without pretending the format is still the primary direction.

## Layout

- **repub.json** (root): Format version, book title, author, entry path, optional dependencies map, build info.
- **content/index.html**: Entry document. It loads scripts and styles from `../assets/`.
- **assets/**: Pre-built JS and CSS. No node_modules; everything is bundled at build time.
- **meta/** (optional): Cover image, description, for catalog/UI.

## repub.json

- **formatVersion**: Integer (e.g. 1). Readers reject unknown versions.
- **title**: Book title.
- **author**: Optional.
- **entry**: Path to entry HTML (e.g. content/index.html).
- **dependencies**: Optional. Map of package name to version. Declarative only; used for docs/audit.
- **buildInfo**: Optional. builderVersion, buildTime (ISO 8601).

## Dependencies

The dependencies object describes what the author used when building. It's typically derived from the repub project's package.json at pack time. The actual .repub does not contain node_modules; resolution and bundling happen in the builder. The reader never fetches npm.

## Versioning

- **formatVersion**: Bump when the container or manifest schema changes in a backward-incompatible way.
- **builderVersion**: Identifies the tool that produced the file (for debugging).

## Conclusion

The specification is still worth preserving as an artifact, but it now serves as reference material rather than roadmap. The live work moved to better tooling and standard output instead of continued container design.

## Examples

- The layout and `repub.json` sections are the clearest concrete examples of how the old container was structured.
- The versioning notes are the example of the operational overhead that comes with maintaining a custom format.

The project is no longer an active part of the docs surface here, but the archive note remains useful if you need to reference the container shape later.
