---
title: "global — Task Registry"
description: "Auto-compiled from ./.planning/TASK-REGISTRY.xml"
generated: "true"
source: "./.planning/TASK-REGISTRY.xml"
updated: "2026-04-09T02:21:04.772Z"
---

# Task Registry

## Phase 01

- ✓ **01-01** Migrate narrative from removed .planning-archive/2026-03-29-pre-barebones/ into apps/portfolio/content/docs/documentation/requirements.mdx (Planning archive section) and registries; ROADMAP.xml + TASK-REGISTRY.xml updated; archive folder deleted.
- ✓ **01-02** Add a shared logging layer for server and client flows, then instrument the assistant-ui chat path so request, retrieval, upstream, and error handling are easy to debug without permanent ad hoc console logs.
- ✓ **01-03** Implement the blog and projects content-discovery pass after the planning loop: searchable/filterable index pages, command-palette hit flow, strict article/project structure, and matching docs-side records.
- ✓ **01-04** Books EPUB planning supplement: repeatable `repub epub --planning &lt;dir&gt;`, `runEpub` appendix for md/mdx/xml/toml/txt, `scripts/build-books.cjs` reads `book.json` epubPlanningDirs; Mordred&apos;s Tale bundles `apps/portfolio/content/docs/books/planning`. Section registry: `books-reader-02-02` done; docs in books planning-docs.mdx.
- ✓ **01-05** Phase 01 CLI baseline: planning --version (repo-planner package.json), repub --version, engines.node &gt;= 22 on root + @portfolio/repub-builder + vendor/repo-planner; --help/subcommands already in loop-cli; Ink TTY + npm publish + pnpm dlx matrix remain documentation-site-07 / repo-planner-integration-01-13.
- ✓ **01-06** Retrofit the existing blog posts and project writeups to the strict literal section contracts and richer metadata expected by the new discovery shell, then lock that contract with a regression test.
- ✓ **01-08** Repo Planner phase 04: build-time `builtin-packs.json` from `.planning` + docs MDX; `repo-planner` exports `PlanningPackGallery` (data-in props); portfolio modal shell only; cockpit preloads init + docs packs; root `pnpm planning:embed-packs`; hook into app dev/build. Registry: repo-planner-integration-04 in apps/portfolio/content/docs/repo-planner/planning/task-registry.mdx.
- ✓ **01-07** Normalize `.planning` and operator-facing planning docs to an ASCII-first punctuation policy so CLI, shell, and XML surfaces stay readable without changing public-facing prose typography.
- ✓ **01-09** Planning IA reset: move section planning docs into literal `planning/` folders, align root/operator guidance on roadmap/state/task-registry/decisions as the living loop, sweep stale flat links, and verify the planning-pack/build pipeline still resolves the new paths.
- ✓ **01-10** Standardize the agent loop around a Global-first human entrypoint, kickoff records for vague phases, explicit definition of done, and tests-required-for-done for executable behavior; seed RepoPlanner planning for workflow-aware snapshot, kickoff scaffolding, done gates, and ownership guidance.
- ✓ **01-11** Establish the shared auth foundation for gated site features: Payload auth-enabled owner user, tenant/org seed, long-lived session policy, local auto-login in development, and entitlement helpers that Books and Listen can consume.
- ✓ **01-12** Portfolio + vendored repub-builder duplicate-pattern pass: central `lib/unknown-error` + `lib/tsx-json-worker` for timeout JSON stdout workers; stdin workers reuse `resolveTsxCliPath`; RAG ingest CLI + provider-bootstrap use shared unknown-error string; repub-builder `src/utils/unknown-error` + EpubViewer fetch catch alignment.
- ✓ **01-13** Portfolio unknown-value dedupe: `isUnknownRecord`; `coerce-unknown-to-string` (`coerceUnknownToString`, `unknownToStringStrict`, `normalizeOptionalTrimmedString`, `normalizeOptionalTrimmedStringStrict`); `lib/coerce-unknown.ts` (`unknownToBoolean`, `unknownToFiniteNumber`, `parseUnknownFiniteNumber`); `read-json-stdin` for workers; replace scattered guards/coercions across reader workspace, published-book-artifacts API, seed sync, publish scripts, site-branding, listen seed; unit tests exercised for touched modules.

## Phase 02

- → **02-01** Plan and land the shared data/storage provider abstraction in the Global docs + codebase so Payload/SQLite local workflows and Supabase hosted workflows share one runtime contract, repository-backed runtime access, bootstrap path, and verification story without committing large `.db` files.

## Phase 03

- ✓ **03-01** Extract `@portfolio/repub-builder` into the standalone `MagicbornStudios/repub-builder` repository, vendor it back into this repo as `vendor/repub-builder`, and keep the monorepo consuming it as a working workspace dependency and git-installable package.
- → **03-02** Classify the large git blobs by artifact type, land the first off-repo publish path for versioned EPUB/planning-pack artifacts, define what moves into storage/database versus what remains repo-resident, document the branch-history cleanup or rebuild required because the current unpushed range already contains large DB/EPUB blobs, restore builtin planning-pack visibility in the portfolio modal, and keep applying the SSR/SEO-safe rendering contract through real migrations for heavy public media and DB-backed public content.
- ✓ **03-03** Execute the next public-catalog migration after the storage baseline: `site-app-records` plus `site-download-assets` back `/apps`, `project-records` now supply project metadata while repo-authored MDX bodies stay canonical, and `resume-records` now drive the resume gallery while `site-download-assets` keep serving planning-pack and resume artifacts with repo fallbacks where planned.
- ✓ **03-04** Refresh the public shell around the existing owner-only auth/session contract so the sidebar exposes login/account affordances, avatar, live user/org context, logout, and private/admin entry points only for the seeded owner account.
- ✓ **03-05** Mount the upstream Payload `(payload)` app route group as the owner-only operational admin surface, adapt it to this app's config/runtime, and use it for CRUD on Payload-backed resources instead of inventing a second owner back office.

## Phase 05

- ✓ **05-01** Finish the cross-project observability pass: wire privacy-aware Vercel Analytics into the portfolio and Grime Time public layouts, add a small Grime Time observability helper for request ids plus safe route summaries, cover the helper and response-header behavior with tests, and verify with targeted typecheck/lint/test/build commands.

## Phase 06

- ✓ **06-01** Root CLI alignment: `pnpm gad` / `pnpm planning` invoke GAD; `pnpm planning:snapshot` uses `gad snapshot --projectid global`; legacy RepoPlanner (`run-legacy-repo-planner-cli.mjs`) only for init, embed-packs, checklist, report-view until GAD replacements exist. Deprecation stderr on legacy. Prune `run-planning-cli.mjs` to shim.
- ✓ **06-02** CLOSED — no shipped `packages/gad-ui` npm package: indefinite deferral. Placeholder README in `vendor/get-anything-done/packages/gad-ui/README.md` documents non-commitment and points to archival RepoPlanner/portfolio UI for reference only. (A future GAD-lite / UI is research-only.)
- → **06-03** RepoPlanner → GAD migration: `skills/rp-*` and `.claude/skills/rp-*` are thin stubs to `vendor/get-anything-done/skills/`; policy and history in `.planning/REPOPLANNER-TO-GAD-MIGRATION-GAPS.md` (upstream RepoPlanner: `main` = pre-skills framework; `development` = skills-era line). **Remaining:** remove `repo-planner` from apps/portfolio (re-exports, planning-state APIs, live-bundle), migrate to GAD-backed flows; update docs IA and site registry; archive upstream RepoPlanner read-only when imports are gone. **UI track (planned):** rebuild portfolio planning cockpit/embed components in `vendor/get-anything-done` and import from submodule; `/docs` already GAD-shaped — replace RP-framed embeds. Discuss package boundary, phased scope, data contract, and verification checklist before implementation — see `REPOPLANNER-TO-GAD-MIGRATION-GAPS.md` §2a and `/docs/repo-planner/planning/planning-docs` (GAD-native cockpit rebuild).
- ✓ **06-04** Upstream RepoPlanner `apps/landing` Vercel: keep `npm run build` green — direct deps for embed-transitive peers; `scripts/link-landing-deps-to-repo-root.mjs` on postinstall+prebuild symlinks landing `node_modules` packages into repo-root `node_modules` so `tsc` resolves `react`/`next`/`@types` from `components/**` (hoisting alone is insufficient). Document in REPOPLANNER-TO-GAD-MIGRATION-GAPS + decision **06-01**.
