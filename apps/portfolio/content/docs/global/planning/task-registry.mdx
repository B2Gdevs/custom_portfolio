---
title: Task Registry
description: Executable tasks for the Global docs section and repo-wide planning IA.
date: "2026-03-28"
updated: "2026-04-06"
order: 3
repoPath: apps/portfolio/content/docs/global/planning/task-registry.mdx
taskPhase: global-data-02
---

# Task Registry

## Phase `global-site-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-site-01-01` | `done` | **Global** section uses same planning layout as other sections (`planning-docs`, `state`, `task-registry`, `decisions`); [Global planning guide](/docs/global/global-planning) is a **reference** page (graphs, layers, pack policy) | `-` | docs tree shows `global/planning/` + `global-planning.mdx` sibling |
| `global-site-01-02` | `done` | Cross-link sweep: section `planning-docs` / books / repo-planner point at Global **planning docs** + **global planning guide** where needed | `01-01` | grep + build |
| `global-site-01-03` | `done` | Cockpit workspace: **This repository** + uploaded packs, `localStorage`, export JSON -- [`/docs/apps/repo-planner`](/docs/apps/repo-planner) | `-` | shell + dynamic `PlanningCockpit` |

## Phase `global-site-02`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-site-02-01` | `done` | make the Global docs section the human-first planning entrypoint and align root `AGENTS.md` / `.planning/AGENTS.md` with that read order | `global-site-01-01` | `pnpm run build`, `pnpm run lint` |
| `global-site-02-02` | `done` | define the standard phase loop: kickoff for underdefined phases, explicit definition of done, and tests required for done on executable behavior | `global-site-02-01` | `pnpm run build`, `pnpm run lint` |
| `global-site-02-03` | `done` | plan the next RepoPlanner feature phase around workflow-aware snapshot, kickoff scaffolding, done gates, and global-vs-section guidance | `global-site-02-02` | `pnpm run build`, `pnpm run lint` |

## Phase `global-auth-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-auth-01-01` | `done` | establish Payload auth foundations for the site: auth-enabled users, tenant/org seed, owner seed, long-lived session cookie policy, no public signup, and local auto-login for development | `global-site-02-02` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `global-auth-01-02` | `done` | add shared tenant and entitlement helpers so books/listen/admin features can ask one permission layer whether private or write-capable behavior is allowed | `global-auth-01-01` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `global-auth-01-03` | `done` | expose the auth foundation to the portfolio host: login, logout, me/session checks, and owner-aware gating helpers without opening public signup | `global-auth-01-01` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |

## Phase `global-auth-02`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-auth-02-01` | `done` | refresh the portfolio shell/sidebar around the existing owner-only auth/session contract: add shadcn-compatible login/account affordances, keep public browsing open, show live user/org context plus avatar/logout in the sidebar footer, and surface private/admin entry points only when the seeded owner session is active | `global-auth-01-03`, `global-data-03-01` | relevant component tests, `pnpm run build`, `pnpm run lint` |

## Phase `global-auth-03` (restricted operators + visitor reader policy)

**Goal:** **Public site stays public**, but **image generation, Copilot editing workflows, and other mutation paths** are limited to **tenant owner and org admins**. **Logged-in non-admin** users (when invites exist) are **read-only** on site content and **short-lived / export-first** in the reader. **Invites + privilege matrix** are **phase-2** design tasks until scheduled.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-auth-03-01` | `done` | gate **`POST /api/media/generate`** behind **session** + **`admin:access`** entitlement; **401** unauthenticated, **403** member without admin | `global-auth-01-03` | `tests/unit/api/media-generate-route.test.ts`; SiteCopilot uses **`credentials: 'include'`** |
| `global-auth-03-02` | `planned` | **Visitor / non-privileged reader:** annotations and local reader state stay **browser-local** or **session-scoped**, **no durable server sync** unless entitled; UX and copy push **export**; align with [books requirements](/docs/books/requirements) **`BOOKS-REQ-08`** / **`BOOKS-REQ-10`** | `global-auth-03-01` | reader tests or documented UAT |
| `global-auth-03-03` | `in-progress` | **Invites + org roles:** trusted **`pnpm auth:invite`** operator flow plus **single-use, expiring, tenant-scoped invite links** for consumers; role templates stay **`owner` / `admin` / `member`** with **default-deny entitlements**; product self-signup stays out of scope. Docs: [PLAN](/docs/global/planning/plans/global-auth-03/plan), [INVITES](/docs/global/planning/plans/global-auth-03/invites) | `global-auth-01-01` | run invite script against dev DB; accept/revoke/expiry test cases from [PLAN](/docs/global/planning/plans/global-auth-03/plan) |
| `global-auth-03-04` | `in-progress` | **Clerk integration for multi-tenant portfolio auth:** Clerk as identity provider, Payload as authorization core. **Implemented:** `@clerk/nextjs` + `svix` deps, `externalIds.clerkId` on users, `externalIds.clerkOrgId` on tenants, `ClerkProviderWrapper`, `middleware.ts`, `/api/webhooks/clerk` (user/org sync), `/api/auth/clerk-session` (lookup by clerkId), `use-clerk-auth.ts` hook, `SidebarAccountSection` with Clerk SignInButton, `scripts/link-clerk-user.ts`. **Remaining:** `pnpm install`, link owner to Clerk org `org_3Bn9AgebPYXukl8FbjnhN0Tff9V`, verify build, test sign-in. Docs: [CLERK](/docs/global/planning/plans/global-auth-03/clerk) | `global-auth-03-03`, `global-auth-01-01` | `pnpm install && pnpm run build`; `scripts/link-clerk-user.ts`; manual Clerk sign-in shows owner role + tenant |
| `global-auth-03-05` | `planned` | **Provider-agnostic orgs + sync planning:** define first-party organizations, memberships, role templates, entitlement locks, provider mappings, and outbound domain events so the portfolio can adopt hosted identity without making Clerk the authorization source of truth. Includes org/provider sync rules, external-id mapping, and conflict handling for multi-tenant membership changes. Docs: [ORG-SYNC](/docs/global/planning/plans/global-auth-03/org-sync) | `global-auth-03-04`, `global-auth-03-03` | planning notes updated; decision records provider-authority split and event contract |

## Phase `global-tooling-01` (Magicborn CLI - backlog)

**Goal:** unified operator CLI for **generate**, **style/prompt architecture**, and **vendor** workflows - see [kickoff](/docs/global/planning/plans/magicborn-cli/plan). **Operator book RAG** (prompt assembly only) stays **`global-tooling-dec-01`** and is **never** mixed into default site chat RAG (**`global-data-dec-09`**). **`magicborn batch`** is explicitly **out of scope** until **`01-04`** proves single-target flows.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-tooling-01-01` | `done` | scaffold `magicborn` / `pnpm mb` via **`@magicborn/cli`**: `generate` (book, app, planning-pack, listen) delegates to `scripts/magicborn/run.ts`; `vendor add` (dry-run + `--apply`) | `-` | `pnpm magicborn --help`; `pnpm magicborn generate book --print-prompt` prints style+scene; `pnpm magicborn vendor add <url>` prints plan |
| `global-tooling-01-02` | `done` | CLI **`pnpm magicborn generate <target>`** calls **`lib/magicborn/openai-image-generate`** (same behavior as **`POST /api/media/generate`**) using **`.env`** (`OPENAI_API_KEY`); writes **`.magicborn/runs/<id>/`** (`image.png`, `prompt.txt`, `manifest.json`); **rag-style** terminal UI when **`MAGICBORN_CLI=1`**; flags **`--dry-run`**, **`--print-prompt`**, **`--raw`**, **`--size`**, **`--slot`**, **`--json`**; spend line when OpenAI returns **usage** | `global-tooling-01-01`, `global-auth-03-01` | `tests/unit/lib/magicborn-openai-image-generate.test.ts`; `tests/unit/api/media-generate-route.test.ts`; dry-run without key |
| `global-tooling-01-03` | `planned` | **Asset taxonomy** in data model: `kind` (site, book, app, project, listen, planning-pack, blog, ui), **ref** + **variant**, **`textOnImage`** (or equivalent), storage URL + hashes; wire **`site-app-records`** and **`project-records`** to generated media; **reconcile** local manifest vs Payload for partial runs and manual upload | `global-tooling-01-02` | loader tests + `pnpm run build`; at least one app + one project record can point at a generated asset |
| `global-tooling-01-04` | `planned` | **Site logos:** generate variants, upload to **`site-media-assets`** (or sibling collection), CLI commands to **list** candidates and **`set-active`** logo pointer the app shell/header reads (**`global-tooling-dec-03`**) | `global-tooling-01-03` | E2E or unit: active pointer resolves; CLI documented in [magicborn-cli plan](/docs/global/planning/plans/magicborn-cli/plan) |
| `global-tooling-01-05` | `done` | **CLI visual language + completion UX docs:** semantic colors (verb / arg / flag / section / log levels), domain ribbons (**`planning-pack`** vs **`book`** vs ...), long-running spinner for OpenAI; bash **`print-completions-horizontally off`** + zsh/fish notes for list-friendly completion; fix bash top-level **`payload`** word | `global-tooling-01-02` | [magicborn-cli PLAN](/docs/global/planning/plans/magicborn-cli/plan) "CLI visual language"; `tests/unit/lib/magicborn-cli-ui.test.ts`; `pnpm exec vitest run` (portfolio) |
| `global-tooling-01-06` | `done` | **Grouped color `magicborn --help`** (payload / vendor / OpenAI / model / shell / asset sections + dim magenta descriptions); **warnings -> stderr** in portfolio CLI UI; ribbons align with help (**yellow** asset family, **cyan** Payload catalog); **Grime Time** `PageMediaRegistryBridge` effect keyed by entry signature + ref (fix max update depth) | `global-tooling-01-05` | `packages/magicborn-cli` tests + build; `pnpm magicborn --help`; vendor `PageMediaDevtoolsContext` + int test |

## Phase `global-tooling-02` (mb-cli-framework - shared shell)

**Goal:** vendor **[mb-cli-framework](https://github.com/MagicbornStudios/mb-cli-framework)** as `vendor/mb-cli-framework`, share completion / scope / color conventions between **`magicborn`** (this repo) and **`grimetime`** (Grime Time), tab-first DX, **`vendor use` / `vendor clear`**, and a roadmap for Payload/OpenAI/RAG command discovery. Kickoff: [PLAN](/docs/global/planning/plans/mb-cli-framework/PLAN).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-tooling-02-01` | `done` | **Submodule + workspace:** `git submodule add` -> `vendor/mb-cli-framework`; `pnpm-workspace.yaml` entry; minimal package skeleton in submodule (or initial commit upstream) so both repos can `workspace:*` or `file:` depend | `global-tooling-01-01` | `pnpm install`; `git submodule status` |
| `global-tooling-02-02` | `done` | **Tab completion:** `vendor-suggest` topic + **`(cli)`** suffix / optional ANSI (**`NO_COLOR`**, **`FORCE_COLOR`**, **`MAGICBORN_COMPLETE_ANSI`**); bash completion + fish hints; legend in completion script header | `global-tooling-02-01` | `tests/unit/magicborn-complete-words.test.ts`; `pnpm --filter @magicborn/cli test` |
| `global-tooling-02-03` | `done` | **Scope:** `magicborn vendor use <id>` / `clear` / `scope` (`.magicborn/vendor-scope.json`); **`MAGICBORN_VENDOR_ROOT`** when forwarding | `global-tooling-02-01` | `pnpm --filter @magicborn/cli test` (`vendor-scope`, `vendor-run`); manual use/clear |
| `global-tooling-02-04` | `done` | **Port consumers:** `@magicborn/cli` depends on **`@magicborn/mb-cli-framework`**; portfolio **`complete-words`** imports framework + **`getVendorCompletionRows`** | `global-tooling-02-02`, `global-tooling-02-03` | `pnpm --filter @magicborn/cli build`; `pnpm --filter @portfolio/app exec vitest run tests/unit/magicborn-complete-words.test.ts` |
| `global-tooling-02-05` | `done` | **Discovery spike:** `magicborn payload collections` lists slugs from **`payload.config`** (**`--json`**); completion topic **`payload`** -> **`collections`** | `global-tooling-02-04` | `scripts/magicborn/run.ts` branch; `tests/unit/scripts/magicborn-payload-collections.test.ts` |

## Phase `global-tooling-03` (Ink TUI + vendor v2 + chat + themes)

**Goal:** **Ink**-based terminal shell (layouts, borders, list/detail, forms), optional **[assistant-ui for Ink](https://www.assistant-ui.com/ink)** operator chat shared via **`mb-cli-framework`**, **shadcn-aligned theme tokens** export, **vendor** UX that lists **real vendor packages** (badges for CLI hop) instead of grimetime-only top-level aliases, and **`payload <alias> generate`** (+ guided chat). Kickoff: [ink-tui-vendor-v2](/docs/global/planning/plans/magicborn-cli/ink-tui-vendor-v2).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-tooling-03-01` | `done` | **`mb-cli-framework`:** Ink host entry (`renderPlain` vs `renderInk`), **theme contract** (token map + `NO_COLOR` / `FORCE_COLOR`), lazy dynamic import for TUI | `global-tooling-02-04` | unit tests for theme + plain fallback; grimetime consumer compiles |
| `global-tooling-03-02` | `done` | **assistant-ui + Ink:** shared `<OperatorChat />` (or equivalent) + tool slot; **magicborn** subcommand e.g. `magicborn chat` or flag `--chat` for guided flows; **CI** path skips Ink | `global-tooling-03-01` | manual: chat opens in TTY; `--json` unchanged |
| `global-tooling-03-03` | `done` | **Default `magicborn` home:** Ink **dashboard** (sections, rules, list) replacing string-only help; keep **`magicborn --help` plain** for pipes / `NO_COLOR` automation | `global-tooling-03-01` | snapshot or component tests; `MAGICBORN_PLAIN` doc |
| `global-tooling-03-04` | `done` | **Vendor v2:** remove **top-level** `users`/`org`/`tenant`/`blog` from Commander (shim + deprecation optional); **`vendor <id>`** tree + **badges**; **`use`** spawns vendor CLI with **`cwd` + .env merge**; completion topics updated | `global-tooling-02-03` | `pnpm --filter @magicborn/cli test`; e2e: `vendor grimetime -- --help` |
| `global-tooling-03-05` | `done` | **`payload app generate`:** alias → `site-app-records`; **`--dry-run`** contract; in-process Payload upsert (**`PAYLOAD_SECRET`** + **`DATABASE_URL`**, same as seed scripts); body from registry **`--slug`**; guided **chat** for missing fields remains future | `global-tooling-02-05`, `global-tooling-03-02` | `tests/unit/lib/payload-cli-generate.test.ts`; `pnpm --filter @magicborn/cli test` |
| `global-tooling-03-06` | `done` | **Ink home polish:** `/` palette **↑↓** selection + **Enter** apply/run; **`magicborn env`** under shell/meta — shows effective vendor scope + **`vendor/.env`** merge (redacted) matching nested **`magicborn vendor <id> …`** child env; **`--json`** | `global-tooling-03-03`, `global-tooling-02-03` | `pnpm --filter @magicborn/cli test`; manual `magicborn env` + REPL `/` + arrows |
| `global-tooling-03-07` | `done` | **`magicborn chat` prod path:** merge repo-root **`.env` / `.env.local`** into **`process.env`** before config snapshot + **`pnpm … build`** (same fill rules as **`applyMonorepoEnvFromRepoRoot`** in **`next.config`**); **`chat:build` / `pnpm` spawn** uses **`shell: true` + single command string** on Windows to avoid **`spawn EINVAL`** with **`pnpm.cmd`** + **`stdio: 'inherit'`** | `global-tooling-03-02` | `pnpm --filter @magicborn/cli test` (`monorepo-dotenv`); manual **`magicborn chat`** from Git Bash on Windows |

## Phase `global-tooling-04` (Ink — Claude Code–style chat shell)

**Goal:** **Chat-first** Ink UX shared via **`mb-cli-framework`**: bordered **banner** (product name from config + ASCII **M** + **system** + cwd), **slash** commands in **purple/magenta**, **transcript** with `> /cmd` + **`└`** outcome lines, **bottom panel** only for pickers (**Enter** / **Esc**), **footer** (next action · model · folder), **interrupt-safe** long jobs (RAG/subprocess) without exiting the shell. **Model** + **RAG index / off** flow from TUI into **`/api/chat`** (headers or body). Kickoff: [KICKOFF](/docs/global/planning/plans/magicborn-cli-ink-claude-ux/KICKOFF).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-tooling-04-01` | `planned` | **Primitives + theme:** extend **`CliTheme`** with **`slash`** (and optional **`banner` / `footerAccent`**) + **`NO_COLOR`** rules; **`mb-cli-framework`** exports **`<ClaudeStyleShell>`** (or composable **Banner**, **FooterBar**, **SlashTranscript**, **BottomPanelHost**); document token map (not “local, assets, shell” ad hoc) | `global-tooling-03-06` | unit tests theme + layout smoke; grimetime consumer compiles |
| `global-tooling-04-02` | `planned` | **Slash router + history:** parser/registry for **`/model`**, **`/rag`** / **`/index`**, **`/rag off`**; in-memory + **`.magicborn/cli-session.json`** (or similar) for last model + index; transcript reducer for **`>`** / **`└`** lines | `global-tooling-04-01` | vitest: parser + reducer; manual slash flow |
| `global-tooling-04-03` | `planned` | **Portfolio contract:** extend **`SiteChatApiRequest`** JSON with **`model?`** + **`ragMode?`** (`off` \| `books` \| `books_planning` \| `books_planning_repo`); **`POST /api/chat`** honors per-request **`ragMode: off`** and model override; **OpenAI** `models.list` for Ink picker (filtered); **ingest/retrieve** must gain corpus filters or stores for each mode (may trail UI) — see [KICKOFF](/docs/global/planning/plans/magicborn-cli-ink-claude-ux/KICKOFF) | `global-tooling-04-02`, `global-data-dec-09` (RAG boundary) | unit route test + manual chat with `ragMode` + model |
| `global-tooling-04-04` | `planned` | **Interrupt + jobs:** **`AbortController`** for fetch; optional **child process** wrapper for RAG ingest with **Ctrl+C →** transcript “Interrupted · …” and return to composer | `global-tooling-04-02` | manual: interrupt mock long request |
| `global-tooling-04-05` | `planned` | **Footer row 2 + session policy:** **context window %** bar (token estimate from messages + system + tool payloads); **auto-compact** over threshold; **`approve edits`** vs **`auto accept edits`** toggle (persist session); session **tool-run counter** optional — see [Ink kickoff](/docs/global/planning/plans/magicborn-cli-ink-claude-ux/KICKOFF); refine estimates once **`05-02`** returns large tool JSON | `global-tooling-04-01` | vitest token math; manual toggle + compact |

## Phase `global-tooling-05` (Copilot — Payload tools, forms, creative batches)

**Goal:** In chat, **query and discuss** allowlisted **Payload** data via **server-side tools** (accurate, paginated); **schema-driven Ink forms** for create/update; **multi-turn clarification**; **batch media** flows (project style → image \| video → scenes from picker or **book/planning extraction**) reusing **`magicborn` / `.magicborn/`** paths. **Hybrid context:** DB tools for truth; **swappable `ragMode`** for prose (see **`global-tooling-04`**). Kickoff: [KICKOFF](/docs/global/planning/plans/magicborn-copilot-payload-tools/KICKOFF).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-tooling-05-01` | `planned` | **Read tools:** allowlisted **Local API** queries (`find`, `findByID`, depth/limit); stable JSON shape for the model; no broad GraphQL from free text | `global-admin-01-01`, `global-auth-03-01` (session rules) | unit tests on tool handlers + denylist |
| `global-tooling-05-02` | `planned` | **Tool-use loop:** extend **`/api/chat`** (or **`/api/copilot/turn`**) with OpenAI **tools**; server executes tools and feeds results back; timeouts + max rounds | `global-tooling-05-01` | integration test with mock LLM tool calls |
| `global-tooling-05-03` | `planned` | **Forms:** map **Payload field defs** → **form descriptor**; Ink bottom-panel render + submit envelope; **mutations** only with session + confirmation | `global-tooling-04-01`, `global-tooling-05-02` | one collection E2E create draft |
| `global-tooling-05-04` | `planned` | **Creative batch:** wizard steps style → medium → scenes → call existing **image** generate path; persist **manifest** / links per **`global-tooling-01`** taxonomy | `global-tooling-01-02`, `global-tooling-05-03` | manual batch + artifacts on disk/Payload |
| `global-tooling-05-05` | `planned` | **Scene extraction:** parse **books / planning MDX** for scene-like blocks (headings, markers); optional **RAG** assist for suggestions only; tests on fixture files | `global-tooling-05-04` | unit tests fixtures |

## Phase `global-tooling-06` (Books · series · scenes in DB + EPUB illustration scan)

**Goal:** **Payload** models for **series → book → scene** with **media variant** records (image / video / **scene play** prompts + assets); **fast EPUB scanner** for **illustration pending** slots with **reading-order context windows** (prose visible **before** each figure in LTR layout); feed **Copilot** and **`books-publishing-03`**. Kickoff: [KICKOFF](/docs/global/planning/plans/magicborn-books-scenes-epub/KICKOFF).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-tooling-06-01` | `planned` | **Schema:** collections (or normalized tables) for **Series**, **Book**, **Scene** (FK, ordering), **SceneMediaVariant** (type, prompts, asset ids, status); align access with **`global-auth`** | `global-admin-01-01` | Payload migration + contract test |
| `global-tooling-06-02` | `planned` | **`scanEpubIllustrationGaps`:** zip + XHTML parse; detect placeholder figures; emit ordered **slots** + **contextText** per [reading-order rule](/docs/global/planning/plans/magicborn-books-scenes-epub/KICKOFF) | `books-reader-01-11` (page shell reality) | unit tests on fixture EPUB; `pnpm --filter @portfolio/repub-builder test` where shared |
| `global-tooling-06-03` | `planned` | **CLI / Copilot hook:** `magicborn books illustrations scan <slug>` (or tool) → JSON; optional **queue** rows for batch compose + **`global-tooling-05`** approval gates | `global-tooling-06-01`, `global-tooling-06-02` | script run + snapshot |
| `global-tooling-06-04` | `planned` | **Scene play** field + **video** prompt pipeline stub (store + validate; generation provider TBD) | `global-tooling-06-01`, `books-publishing-03-02` | unit: compose from scene play + style |

## Phase `global-admin-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-admin-01-01` | `done` | mount Payload's owner-only admin surface with the upstream `templates/blank/src/app/(payload)` route group, adapt it to this app's config/runtime, and use it as the CRUD back office for Payload-backed resources instead of inventing a second owner CRUD stack | `global-auth-02-01` | relevant route/component tests, `pnpm run build`, `pnpm run lint` |

## Phase `global-data-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-data-01-01` | `done` | define one shared provider contract for app data and uploaded assets, including env selection rules and phase-1 boundaries for local SQLite/file mode versus hosted Supabase mode | `global-auth-01-03` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `global-data-01-02` | `done` | move shared Listen and Reader runtime access behind provider-owned repositories/bootstrap services so routes and components stop depending on one concrete backend | `global-data-01-01` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `global-data-01-03` | `in-progress` | add the install/sync/bootstrap path for large local data: local SQLite/file export-import rules plus Supabase bootstrap/migration flow for hosted data and uploads | `global-data-01-01`, `global-data-01-02` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `global-data-01-04` | `done` | **Site RAG scope (phase 1):** do **not** ingest full book manuscript EPUB text into the shared site chat/RAG corpus by default; that boundary stays aligned with **`BOOKS-REQ-04`** and **[books requirements](/docs/books/requirements)**. **Not a goal:** wiring the reader's EPUB pipeline into RAG. **Docs alignment:** record the decision in Global **decisions**; keep **local vs Supabase** backend notes in **`global-data-01-03`** / [Supabase bootstrap](/docs/global/planning/plans/global-data-01/SUPABASE-BOOTSTRAP) / deploy docs as that track finishes | `global-data-01-01` | `global-data-dec-09` in [decisions](/docs/global/planning/decisions), `pnpm run build`, `pnpm run lint` |

## Phase `global-data-02`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-data-02-01` | `done` | extract `@portfolio/repub-builder` into the standalone `MagicbornStudios/repub-builder` repo, vendor it back as `vendor/repub-builder`, and keep the monorepo consuming it as a working workspace package and git-installable library | `global-data-01-01` | `pnpm --filter @portfolio/repub-builder test`, `pnpm --filter @portfolio/app exec vitest run tests/unit/components/books/ReaderWorkspace.test.tsx`, `pnpm run build`, `pnpm run lint`, submodule repo push |
| `global-data-02-02` | `in-progress` | classify the large git blobs by artifact type, land the first off-repo artifact path (versioned EPUB + planning-pack uploads with manifest-backed remote URLs), and define the branch-history cleanup/rebuild needed because the current unpushed range already contains large DB/EPUB blobs | `global-data-02-01` | storage policy doc + hosted artifact publish checkpoint + `pnpm --filter @portfolio/app exec vitest run tests/unit/lib/book-artifacts.test.ts tests/unit/lib/reader-workspace-state.test.ts tests/unit/lib/payload-collection-contract.test.ts`, `pnpm --filter @portfolio/repub-builder test`, `pnpm run build`, `pnpm run lint` |
| `global-data-02-03` | `in-progress` | define and start applying the SSR/SEO-safe rendering contract for content that moves into the database/storage layer: classify which surfaces stay repo-first, which become Postgres-backed server-rendered catalogs, and which binaries/media move entirely to storage; land the new public-media collections/publisher, restore builtin-pack visibility in the planning-pack modal, and wire storage-backed URLs back into the portfolio app without rewriting authored content | `global-data-02-02` | docs updated, route/data matrix reviewed, `pnpm --filter @portfolio/app exec vitest run tests/unit/lib/planning-pack-modal-data.test.ts tests/unit/lib/public-media.test.ts tests/unit/lib/payload-runtime-config.test.ts tests/unit/lib/payload-collection-contract.test.ts`, `pnpm run build`, `pnpm run lint` |

## Phase `global-data-03`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `global-data-03-01` | `done` | add the next public catalog collections and SSR-safe loaders: `site-app-records` plus `site-download-assets`, migrate `/apps` off the hard-coded component array, seed the hosted app registry, and start moving public planning-pack/download metadata away from repo-resident binaries | `global-data-02-03` | targeted loader/component tests, `pnpm run build`, `pnpm run lint` |
| `global-data-03-02` | `done` | move project metadata, ordering, link groups, and media/download relations into `project-records` while keeping long-form project bodies repo-first until the DB path proves simpler | `global-data-03-01` | targeted loader/component tests, `pnpm run build`, `pnpm run lint` |
| `global-data-03-03` | `done` | move resumes onto `resume-records` plus `site-download-assets` so gallery metadata and downloadable HTML/PDF artifacts stop depending on local directory scans and repo-resident files | `global-data-03-01` | targeted loader/component tests, `pnpm run build`, `pnpm run lint` |

## Registry rules

| Field | Value |
| --- | --- |
| `status-set` | `planned`, `in-progress`, `done`, `blocked` |
| `verification` | smallest proof that matches the row |
