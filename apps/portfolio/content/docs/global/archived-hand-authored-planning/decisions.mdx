---
title: Decisions
description: Stable decisions for the Global docs section and repo-wide planning IA.
date: "2026-03-28"
updated: "2026-04-07"
order: 4
repoPath: apps/portfolio/content/docs/global/planning/decisions.mdx
taskPhase: global-site-01
---

# Decisions

| Id | Status | Decision | Impact |
| --- | --- | --- | --- |
| `global-site-dec-01` | `accepted` | **Global planning guide** (`global-planning.mdx`) is a **reference** doc, not part of the collapsed **planning** subtree. The Global planning section itself remains a normal section with requirements, roadmap, state, task-registry, decisions, and a compact playbook. | Sidebar keeps `global/planning/*.mdx` and `global-planning.mdx` distinct without hiding the Global section's planning records. |
| `global-site-dec-02` | `accepted` | The **Global** section is the human-first planning entrypoint for this repo; agents read it before picking a section or consulting `.planning/*.xml`. | Humans and agents share one visible workflow entrypoint on the docs site. |
| `global-site-dec-03` | `accepted` | New, ambiguous, or stale phases should use a lightweight kickoff record before broad implementation starts. | Agents must clarify scope, non-goals, risks, tests, and first tasks before doing large work. |
| `global-site-dec-04` | `accepted` | Executable behavior is not `done` without automated tests. Docs-only or content-only phases still need concrete verification commands, but they cannot waive test requirements for shipped behavior. | Definition of done now includes tests as a hard gate for implementation phases. |
| `global-auth-dec-01` | `accepted` | The site has no public signup in v1. One owner account and one tenant/org are seeded locally, anonymous visitors may still browse public surfaces, and gated features are unlocked by shared auth plus entitlements rather than section-specific passwords. | Books and Listen can add private or write-capable features without inventing separate access models. |
| `global-auth-dec-03` | `accepted` | The repo should use two admin surfaces with different jobs. The public site keeps a custom owner-aware shell for identity, avatar, org context, and private entry points, while owner-only CRUD for Payload-backed resources mounts the upstream Payload admin route group and is customized there. Static SEO-first docs, blog posts, and project pages remain repo-authored rather than becoming deployed CMS content. | The app can feel SaaS-like without pretending every editable surface belongs in the database, and owner operational CRUD can reuse Payload instead of duplicating its admin primitives. |
| `global-auth-dec-04` | `accepted` | Phase-one account UI should source avatar data from the shared users collection and session payload immediately instead of shipping initials-only fallback as the primary plan. Initials remain the visual fallback when no avatar URL is set. | The sidebar and future owner surfaces can show a real SaaS-style account card now, while the auth/session contract stays backend-agnostic and ready for broader org users later. |
| `global-admin-dec-01` | `accepted` | The first mounted Payload admin pass should expose the owner-managed collections that behave like operational content surfaces and hide internal reader-state and RAG maintenance collections until their workflows are intentionally designed. Public-read asset collections may keep public read access while still allowing owner writes through the admin. | `/admin` lands as a focused owner back office instead of a dump of every internal record table, and the existing app/runtime paths keep working while Payload becomes the CRUD surface for the collections that are actually meant to be edited there. |
| `global-data-dec-01` | `accepted` | Shared app data and uploaded assets should move behind a provider contract that keeps local SQLite plus local files as the default development path while adding Supabase as the first hosted backend; the current auth/session layer stays separate for the first pass. | The repo no longer needs committed `.db` artifacts or Git LFS to share runtime state, and section planners can target one backend-neutral contract instead of forking persistence stories. |
| `global-data-dec-02` | `accepted` | The first hosted Supabase bootstrap uses one linked project ref, a private `reader-library` storage bucket, env-driven Payload Postgres/S3 settings in the repo, and a machine-local Codex MCP registration; only the database password and S3 access-key pair stay manual secrets. | Hosted setup becomes mostly mechanical instead of an undocumented dashboard ritual, while local SQLite/file mode remains available when the hosted secrets are absent. |
| `global-data-dec-03` | `accepted` | `@portfolio/repub-builder` should stop living as a normal package directory inside the portfolio monorepo and instead become its own standalone git-installable repo, vendored back into the monorepo as a submodule workspace dependency. | The package can evolve like a real external library, the monorepo keeps a vendored known-good checkout, and future consumers can install from GitHub without depending on this app repo layout. |
| `global-data-dec-04` | `accepted` | Published book binaries should move to storage as immutable versioned artifacts: each build can publish an EPUB plus a zipped planning pack under a version tag, Payload tracks the current version, and `public/books/manifest.json` keeps only lightweight metadata plus the current remote URLs. | Repo history stops being the normal home for shipped EPUB/zip binaries, checkpoints stay explicit, and SSR/SEO-facing routes can still render deterministic metadata without reading heavy files from git. |
| `global-data-dec-05` | `accepted` | Public SEO surfaces may be repo-first or Postgres-backed, but they must render from stable server-side slug and metadata loaders. Supabase storage is for heavy binaries, originals, downloadable bundles, and private uploads; it is not the canonical source for crawlable page copy. | The repo can move heavy files and mutable catalogs off git without giving up SSR or turning public content into client-only fetches. |
| `global-data-dec-06` | `accepted` | Public site media should use a lightweight manifest bridge during migration: upload the heavy files into `site-media-assets`, write a small manifest that maps legacy `/images/...` paths to storage-backed URLs, and resolve those paths inside the content loaders so authored MDX/frontmatter can keep their stable references while the binary files move out of the main runtime path. | The app can adopt storage-backed media without rewriting every existing project/blog document at once, and the same pattern can be extended to future public-media classes or retired later if content moves fully DB-backed. |
| `global-data-dec-07` | `accepted` | The next public catalog migration should separate metadata from heavyweight files: `/apps`, `/projects`, and `/resumes` may move their ordering, summaries, links, and download relations into Postgres collections (`site-app-records`, `project-records`, `resume-records`), while the actual downloadable or media-heavy artifacts move into storage collections such as `site-download-assets`. Long-form editorial bodies can stay repo-first until moving them would truly reduce maintenance. | This keeps SSR and SEO strong, shrinks the repo's file and binary burden, and avoids forcing every public content surface into the database before the metadata and storage split is paying for itself. |
| `global-data-dec-08` | `accepted` | Public planning-pack exports and resume HTML artifacts should publish through the shared `site-download-assets` collection, and site consumers should prefer those published asset URLs while keeping repo-local files as a fallback until metadata migrations are complete. Because the collection is script-published only, it may allow restricted file types such as HTML and markdown without turning into a public upload surface. | One generic download pipeline now covers planning-pack files, resume artifacts, and future public bundles without bespoke buckets or route-specific storage rules. |
| `global-auth-dec-02` | `accepted` | v1 site auth remains owner-only even as the shell is refreshed: public visitors browse almost everything anonymously, but login UI should only unlock private/admin surfaces for the single seeded owner account configured by `PORTFOLIO_OWNER_EMAIL` and related env vars until a broader role model is intentionally added. | The sidebar/login revamp can reuse the existing auth/session API and shadcn-compatible UI patterns without opening public signup or implying multi-user product behavior the backend does not support yet. |
| `global-data-dec-09` | `accepted` | **Phase-1 site RAG** (repo-backed chat / ingest corpus) **excludes full book manuscript text** unless a future phase explicitly opts in. Books-side AI may still ground on **docs and lore** per **`BOOKS-REQ-04`**; the reader EPUB pipeline is **not** required to feed the default RAG index. | Ingest config and operators can treat manuscript EPUBs as out of scope for default **`rag_*`** chunks; avoids duplicating heavy book content in the site vector store while requirements already keep drafts out of default ingest. |
| `global-auth-dec-05` | `proposed` | **Restricted operators:** **image generation** (`/api/media/generate` and related), **SiteCopilot** write/mutation flows, and **Payload owner surfaces** are for **tenant owner + org admins** only until a broader product needs more roles. **Anonymous and non-privileged logged-in** users: **read-only** on public content; **reader** highlights/notes stay **local / export-first** without durable server writes (**[global-auth-03](/docs/global/planning/task-registry#phase-global-auth-03-restricted-operators--visitor-reader-policy)**, **`BOOKS-REQ-09`**, **`BOOKS-REQ-10`**). | Implements a **public portfolio** with a **small trusted operator set**; invites and expanded roles stay **`global-auth-03-03`** until scheduled. |
| `global-auth-dec-06` | `proposed` | **Invite architecture:** the CLI is a **trusted operator** surface and may mint **single-use, expiring, tenant-scoped invite links**. Consumers do **not** use the CLI; they accept invites in-browser, create credentials, and then use the normal session UX. **Payload users + tenants** remain the auth source of truth, while email or OAuth providers stay adapters. | Keeps the consumer auth path simple, avoids provider lock-in, and gives Grime Time a portable user / tenant / role / entitlement core instead of a second auth stack. |
| `global-auth-dec-07` | `proposed` | **Clerk is the leading hosted-identity candidate** for the portfolio's future non-owner auth, but the portfolio should keep **Payload users + tenants + entitlements** as the authorization core. Unlike Grime Time, portfolio adoption needs a planning-first pass because future org membership, tenant scoping, invite acceptance, and section-level product surfaces are multi-tenant rather than single-product customer access. | Lets the repo reuse Grime Time's Clerk lessons without prematurely coupling the portfolio to a customer-portal auth shape that ignores tenants, invites, and section-specific privileges. |
| `global-auth-dec-08` | `proposed` | **Provider-agnostic org core:** the portfolio should keep first-party organizations, memberships, role templates, entitlement locks, provider mappings, and outbound domain events as the business authorization layer. Hosted identity providers such as Clerk may mirror selected org state, but they do not become the canonical source for tenant membership or section-level permissions. | Preserves multi-tenant flexibility, keeps future provider swaps possible, and gives portfolio apps a stable event contract when org/user state changes originate in our admin rather than the identity provider. |
| `global-auth-dec-09` | `accepted` | **Clerk middleware contract (Next.js):** the app’s `middleware` receives **`NextRequest` + `NextFetchEvent`** and passes **both** into the **`NextMiddleware`** instance returned by `clerkMiddleware`—no `as any` event placeholder. **Webhook / scripts:** merging Clerk IDs into Payload uses explicit **`UserExternalIds` / `TenantExternalIds`** shapes instead of `any` on documents. **Client bridge:** `use-auth-session` reads **`window.Clerk?.user?.id`** on an interval so the Clerk session path stays in sync with Payload session fetch. | ESLint-clean Clerk surface; behavior unchanged aside from fixing stale `clerkUserId` in `use-auth-session`. |
| `global-tooling-dec-01` | `accepted` | **Operator media RAG** (CLI / batch prompts grounded on book manuscript or long-form lore) is a **separate ingest and index** from **phase-1 site chat RAG** in **`global-data-dec-09`**. Shared **Magicborn style** (`lib/magicborn-prompts`) is the default universe art-direction layer for generated media; book-grounded retrieval may feed **prompt assembly** without changing the default public-site corpus boundary. | CLI and future jobs can use book context for image/audio/video generation without implying manuscript text ships into the default **`rag_*`** site index. |
| `global-tooling-dec-02` | `accepted` | **Generated media** is classified by **kind** (e.g. `site`, `book`, `app`, `project`, `listen`, `planning-pack`, `blog`, `ui`). **Text rendered inside the image** (title, subtitle, wordmark) is a **per-kind / per-asset** choice, not a global default: e.g. **book covers** and **app/project card art** often include typography; **scene art**, **logos** (unless wordmark), **UI backgrounds**, and many **listen** rows default to **no** burned-in text. | Prompt templates and Payload fields can encode "text on image" explicitly; avoids contradicting the style block's "no text" guardrails when typography is intentional. |
| `global-tooling-dec-03` | `accepted` | **Site logo (and similar global assets):** the CLI may **generate and upload many candidates**, but the **live site** uses an **explicit active pointer** (Payload field, tenant setting, or named manifest key) set by **`magicborn` (or equivalent)**; operators **swap** by changing the pointer, not by overwriting a single canonical file. | Supports archive/reuse and A/B-style swaps without losing history of prior assets. |
| `global-tooling-dec-04` | `accepted` | **CLI env precedence + vendor deprecation:** `magicborn` loads the host repo env once, vendor forwarding applies the vendor `.env` only to the spawned child process, and the old top-level vendor alias commands (`users`, `org`, `tenant`, `blog`) are deprecation shims rather than first-class root verbs. Plain help and `MAGICBORN_PLAIN` stay the automation-safe path when color/TUI is undesirable. | Keeps portfolio and vendor secrets from bleeding together, makes vendor v2 behavior predictable, and records that alias removal is intentional rather than a regression. |
| `global-tooling-dec-05` | `superseded` | **(Withdrawn path.)** ~~Windows Git Bash + **`magicborn claude` / `codex`** forward via **`cmd.exe`** / **`bash -c`**.~~ Portfolio **no longer spawns** those CLIs from Node. | See **`global-tooling-dec-06`**. Historical context: [mb-cli-windows-operator-forward KICKOFF](/docs/global/planning/plans/mb-cli-windows-operator-forward/KICKOFF), [errors-and-attempts](/docs/global/planning/errors-and-attempts). |
| `global-tooling-dec-06` | `accepted` | **`magicborn` does not launch Claude Code or Codex.** Operators install **`claude`** / **`codex`** on **PATH**, **`cd`** into the monorepo (**`cdmb`**, **`MAGICBORN_REPO`**), and run the binary in the **shell**. **`@magicborn/mb-cli-framework`** may still export **spawn helpers** for **other consumers** that opt into their own forwarders. | Avoids unsupported **Node → child TTY** behavior on Windows/Git Bash; scope moves to **DB sandbox**, **MCP/chat** template, and **Payload-first** CLI (**[PAYLOAD-MCP-CHAT-KICKOFF](/docs/global/planning/plans/mb-cli-framework/PAYLOAD-MCP-CHAT-KICKOFF)**). |
| `global-strategy-dec-01` | `accepted` | **Portfolio and Grime Time** get **equal** product attention. **Content sourcing:** **`/docs`** and **blog** stay **repo-first**; **`/projects`** (and related catalog metadata) stay **DB-first** per **`global-data-dec-07`**; **books marketing** stays **repo-first** for now. **Direction:** bring **Grime Time–class** patterns (sectioning, **visual editing**, **media upload + generation**) **heavily** into the portfolio; **extract** reusable **Payload-aligned** UI and workflows into **`@magicborn/mb-cli-framework`** (and/or a thin sibling package) as a **shared dependency** both apps treat like a **vendor**—the framework **owns** those primitives. | Unifies stack assumptions; portfolio catches up on operator UX without forking Payload patterns. |
| `global-strategy-dec-02` | `accepted` | **Copilot / operator writes:** target **org roles** in the product sense, but **today** only the **owner** may trigger **mutating** operator flows until **`global-auth-03`** finishes the role model. **Site RAG / manuscript boundary:** **unchanged** (**`global-data-dec-09`**). **Primary success metric** for scaling the planning loop: **fewer regressions** (verification + decisions + kickoffs), not raw feature velocity alone. | Tightens who can write via tools now; keeps RAG scope stable; optimizes process for reliability. |
| `global-planning-dec-01` | `accepted` | **Planning source of truth:** **`vendor/grime-time-site/.planning/`** is the **default** canonical record for Grime Time work; **root** **`.planning/*.xml`** + **`apps/portfolio/content/docs/global/planning/*.mdx`** hold **monorepo-wide** gates and human narrative. If narratives conflict, **last substantive edit** wins until someone reconciles—**do not** silently fork long-lived truth. **Release cadence:** Grime Time ships on its **own** schedule; portfolio bumps the submodule when ready. | Clarifies where to look first; avoids tying GT releases to unrelated portfolio phases. |
| `global-planning-dec-02` | `proposed` | **Planning sync tooling (future):** agents **lead** keeping **XML ↔ MDX** aligned; humans may override. Longer term: a **planning CLI** (or **`repo-planner`** extension) that **compiles** planning trees from **repo root + vendors** into a **global** view and **fans out** updates with **merge strategies** (e.g. global contributes **requirements** or **requirements + decisions** downward; children may contribute **upward** with flags—**not** git, but **explicit** modes). | Enables monorepo-scale loop without hand-copy drift; needs a **hard cut** contract (follow-on **accepted** row) before implementation. |
| `global-planning-dec-03` | `accepted` | **Kickoff is mandatory** when the **phase goal is vague** (empty, slogan, or multi-interpretable)—stricter trigger than “any ambiguous phase” in **`global-site-dec-03`**. A **kickoff** must list **open questions**; those questions must be **answered or explicitly deferred** before implementation tasks close. | Stops vague roadmap rows from turning into wrong code. |
| `global-planning-dec-04` | `accepted` | **Decisions** row when: (a) a **human** answers a blocking question; (b) a **technology / refactor / UX / feature** choice **unblocks** work; (c) engineering **stops** a path with **evidence** (e.g. **`errors-and-attempts`** + failure mode)—record the **quantitative or criterion-based** reason (what we tried, what failed, what we optimize for next). **`global-tooling-dec-06`** is the reference pattern. | Makes “drop X” and “pick Y” auditable; reduces relitigation. |
| `global-planning-dec-05` | `accepted` | **Verification owner:** the **agent** (or automation) runs **`pnpm` build / lint / tests** per task, updates **`task-registry`**, and only marks **`done`** when checks pass; humans **review** merges and product fit. | Clear accountability for DoD; matches how CI and agents already run. |
| `global-tooling-dec-07` | `proposed` | **Next CLI emphasis:** **`global-tooling-08`** **Ink / operator chat polish** before heavy new surface area. **Onboarding:** aim for **one simple bootstrap command** where it pays for itself; **no** large bespoke installer—**Infisical** (or similar) remains the **env** handoff path. **Payload → CLI:** collections (or fields) should opt into **generated / discovered commands** via **declarative config** on the Payload side (**name TBD**—not `cli-exposed`) when **`@magicborn/mb-cli-framework`** is installed; pair with **agent-facing skills** so a coding agent can extend the CLI from **`OPENAI_API_KEY`** + project config. | Scaffolds repeatable CLI without magic strings; leaves exact schema to a spike (**`global-tooling-09`** or new phase). |
| `global-strategy-dec-03` | `accepted` | **Shared implementation rule:** if portfolio and Grime Time **reimplement the same** Payload/React concern, it **moves into** **`vendor/mb-cli-framework`** (the shared **Magicborn platform** package—**rebrand** away from “CLI-only” over time). A **separate** presentation-only package stays **optional** until the shared tree **hurts** CLI publish size or dependency isolation. | Single vendor-of-record for cross-app UI and hooks; **`oq-01`** default path is **framework first**. |
| `global-data-dec-10` | `accepted` | **Payload + data plane lockstep:** **lock** **Payload major (and compatible minor)** across **portfolio** and **Grime Time**—upgrade on a **defined** cadence (typically when bumping **`vendor/grime-time-site`**). **Supabase:** **separate** projects (or clearly separated schemas/buckets), **shared conventions only** (bucket naming, env keys shape, migration discipline)—no shared production DB between products. | Prevents silent schema drift; keeps blast radius contained per app. |
| `global-env-dec-01` | `accepted` | **Env naming:** use **shared keys** where the variable means the same thing in both apps (**`DATABASE_URL`**, **`PAYLOAD_SECRET`**, **`PAYLOAD_*`**, **`NEXT_PUBLIC_*`** when truly shared). Prefix **app-specific** secrets and deploy targets **`PORTFOLIO_*`** vs **`GRIMETIME_*`**. **Infisical** (or similar) paths should **mirror** that split so operators export the right bundle per app. | One mental model for onboarding; avoids accidental cross-wiring. |
| `global-grimetime-dec-01` | `accepted` | **Visual page composer (phase 13) — MVP slice:** **internal staff** (org **admins** on Grime Time; portfolio analog under **multi-tenant** / **Clerk** org **`org_3Bn9AgebPYXukl8FbjnhN0Tff9V`** for Magicborn). **Must ship:** **media attach**, **AI-assisted copy**, **publish path**, **Service Grid** editing. **Explicit risks to engineer against:** **migrations** breaking live pages and **bad AI edits** (review, undo, drafts). **v2 / nice-to-have** stays **unset** until each candidate feature is named (e.g. full block library, A/B layout variants, non-admin roles). | Focuses GT delivery; portfolio inherits patterns via **`global-strategy-dec-01`**. |
| `global-planning-dec-06` | `proposed` | **GSD alignment fork:** maintain **[MagicbornStudios/get-shit-done](https://github.com/MagicbornStudios/get-shit-done)** as the **tracking fork** of **[gsd-build/get-shit-done](https://github.com/gsd-build/get-shit-done)**; periodically **review upstream commits** and **cherry-pick or document** what we adopt. **Optional:** add **`vendor/get-shit-done`** submodule when we want **in-tree diff** tooling. **We do not** treat full GSD as a runtime dependency of the portfolio app—**inspiration + selective ideas** only. | Tightens loop literacy without binding ship artifacts to GSD releases. |

| `global-planning-dec-07` | `accepted` | **GAD boundary:** `get-anything-done` is the canonical, GUI-free planning methodology — it provides skills, agents, commands, workflows, templates, config, and the `gad` CLI. It never provides a GUI or web surface. **repo-planner** is the official GUI for GAD — it reads what GAD produces and adds visual management, planning discovery, and MDX documentation routing. The RP XML schema (`STATE.xml`, `ROADMAP.xml`) is deprecated in favor of GAD's Markdown format; a `gad migrate-schema` command handles conversion. | All GAD/RP refactor and migration work. |
| `global-planning-dec-08` | `accepted` | **`.gad` pack format:** planning packs are zip archives unpacking to `gad.json` manifest + `commands/`, `agents/`, `workflows/`, `templates/`, `references/` directories. Packs extend GAD for a specific domain (fiction, API dev, web app). They declare `token_estimate` metadata (install tokens, per-phase tokens) used for eval scoring. First-party packs live in `MagicbornStudios/get-anything-done/packs/`. Installed via `gad skills install`. | Pack distribution, eval framework, RP manuscript migration. |
| `global-planning-dec-09` | `accepted` | **Docs sink pattern:** each project owns its `.planning/` dir (source of truth). A separate docs sink (e.g. `apps/portfolio/content/docs/planning/`) holds compiled MDX output. `gad docs compile` is the explicit command — no auto-sync by default (`sink_workflow = "manual"`). The sink is **never** the source; portfolio renders from sink only. | Planning sync MVP, `oq-02` resolution. |
| `global-planning-dec-10` | `accepted` | **GAD eval framework:** knowledge/methodology skills are evaluated by running versioned benchmark projects (requirements-only, local-only, no external services) and measuring requirement coverage, edit efficiency, skill call efficiency, token cost, and document drift. The portfolio's own requirements are the canonical benchmark. Traces (`GAD-TRACE.log`) are written only on eval projects via `--trace` flag. Drift is calculated against a frozen baseline run. | Skill quality measurement, `gad eval` CLI surface. |
| `global-tooling-dec-08` | `accepted` | **mb-cli-framework non-interactive split:** `@magicborn/mb-cli-framework` owns both interactive (Ink/TUI via `OperatorChatApp`) and non-interactive (`TableFormatter`, `JsonEmitter`, `MarkdownEmitter`, `ArgParser`, `ConflictResolver`) utilities. All three CLIs (portfolio `magicborn`, GAD `gad`, grime-time `grimetime`) import from the same framework. GAD CLI and portfolio/grime-time migrate to share the non-interactive utilities rather than reimplementing them. | `gad` CLI build, portfolio non-interactive CLI refactor. |
| `global-planning-dec-11` | `accepted` | **No workspace delete:** removing a GAD project from workspace tracking requires deleting its `.planning/` directory. `gad workspace` provides no delete command. This mirrors `git` — removing the repo is the only way to remove its history. | `gad` CLI scope. |
| `global-planning-dec-12` | `accepted` | **`gad` CLI arg parser:** uses `citty` (`defineCommand` / `runMain`) — already present in grime-time, should migrate into `mb-cli-framework` as the shared non-interactive command layer. Non-interactive path = `citty`. Interactive TUI path = `ink` (already in mb-cli-framework). All three CLIs (portfolio `magicborn`, `gad`, `grimetime`) share this split. | mb-cli-framework non-interactive additions, `gad` CLI build. |
| `global-planning-dec-13` | `accepted` | **`gad` binary name and distribution:** standalone binary named `gad`, published as npm package `get-anything-done` (mirrors GSD's `get-shit-done-cc` naming pattern without the `-cc` suffix). | `gad` CLI packaging. |
| `global-planning-dec-14` | `accepted` | **Eval run isolation:** `gad eval run` creates a git worktree at HEAD (or a specified commit) in a temp directory, runs the agent there, writes versioned output (`evals/<project>/vN/`) back to the main repo, then removes the worktree. This ensures each run starts clean. Baseline can be pinned to a specific commit hash for reproducibility. | `gad eval run` implementation. |
| `global-planning-dec-15` | `accepted` | **gad-manuscript is a separate repo and sub-CLI:** `MagicbornStudios/gad-manuscript` (`vendor/gad-manuscript`) is a domain pack for fiction/book development. It installs as a GAD sub-CLI: when present, `gad manuscript beats`, `gad manuscript scenes`, and book-specific planning commands become available. It has its own eval project (`evals/mordreds-tale-bare/`) based on Mordred's Tale requirements. `rp-manuscript` skill migrates here, broken into GAD-style skills/workflows. gad-manuscript is developed **after** GAD is stable and no longer depends on GSD. | rp-manuscript migration, book development workflow. |
| `global-planning-dec-16` | `accepted` | **Work order for GAD/RP/manuscript migration:** (1) Fix GAD — implement GSD features with our requirements; (2) Move reusable CLI code from portfolio/grimetime/repo-planner into mb-cli-framework; (3) Finish `gad` CLI; (4) Once fully on GAD + mb-cli-framework (GSD no longer needed), begin gad-manuscript; (5) Formalize gad-manuscript with beats/scenes/book-planning CLI. No gad-manuscript work begins while GSD is still the active planning system. | All migration planning. |

| `global-planning-dec-17` | `accepted` | **GAD is a direct GSD replacement.** When GAD is installed, `gad:` commands replace `gsd:` commands entirely. No dual-prefix. Install GAD = install GSD (peer dep) + GAD layer on top. One command, one namespace. | Command naming, install flow. |
| `global-planning-dec-18` | `accepted` | **`planning-config.toml` is the canonical GAD config format.** `[[planning.roots]]` (with `id`, `path`, `planningDir`, `discover`) is the multi-project registry. GAD adds `docs_sink` and `ignore` fields. `gad workspace sync` auto-populates roots. The existing portfolio `planning-config.toml` is the reference implementation. | `gad` CLI, workspace commands, context loader. |
| `global-planning-dec-19` | `accepted` | **`gad:autonomous` is the default GAD workflow** — not a special mode. When a coding agent is given a milestone to work, autonomous handles the full discuss→plan→execute loop. `--only N` for single phase, `--interactive` for human-in-the-loop. | Agent workflow expectations. |
| `global-planning-dec-20` | `accepted` | **Removed commands:** `workstreams`, `list-workspaces`, `new-workspace`, `remove-workspace` (replaced by `gad` CLI), `ui-phase`, `ui-review`, `secure-phase`, `pr-branch`, `ship`, `profile-user` (not core). These are removed from GAD's `commands/gad/` — use the `gad` CLI or native git/gh tooling instead. | GAD command surface scope. |
| `global-planning-dec-21` | `accepted` | **Parallelism default: sequential.** GAD defaults to sequential task execution. Parallel only when tasks are explicitly marked independent AND have no shared write targets (STATE.md, ROADMAP.md). Eval framework measures `parallel_invocations_per_phase`, `token_cost_per_agent`, `conflict_rate`, `skill_activation_waste` to guide parallelism decisions with data. | `execute-phase` wave design, eval metrics. |
| `global-planning-dec-22` | `accepted` | **Nyquist auditor (`gsd-nyquist-auditor`) stays in GAD.** Fills test coverage gaps: for each requirement with no test, classifies type, generates minimal behavioral test, runs it, debugs ≤3 iterations. Pairs with eval document drift detection as the two primary methodology health signals. | `validate-phase`, eval framework. |

## References

- [Planning docs](/docs/global/planning/planning-docs)
- [Global planning guide](/docs/global/global-planning)
