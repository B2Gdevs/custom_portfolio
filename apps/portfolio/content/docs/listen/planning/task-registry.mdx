---
title: Task Registry
description: Compact task records for the Listen section.
date: "2026-03-28"
updated: "2026-03-28"
order: 3
repoPath: apps/portfolio/content/docs/listen/planning/task-registry.mdx
---

# Task Registry

## Phase `listen-surface-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `listen-surface-01-01` | `done` | **Catalog model:** `listen-catalog.ts` -- rows are **`catalogKind: "track" \| "preset"`** with genre, mood, era, BandLab URLs, optional **`lockGroup`**; `music.ts` exposes **public tracks only** for the home section | `-` | `pnpm run build` |
| `listen-surface-01-02` | `done` | **Discovery UX:** **`ListenIndexClient`** on `/listen` -- sidebar filters (type, genre, mood, era, tags), search, sort (newest / oldest / title), mobile sheet; uses **`filterDiscoveryItems`** with **`kind: "listen"`** | `listen-surface-01-01` | `pnpm run build`, `pnpm run lint` |
| `listen-surface-01-03` | `done` | **Unlock:** **`POST /api/listen/unlock`** + **`LISTEN_LOCK_GROUPS`** JSON; **httpOnly** cookie per group; **`router.refresh()`** after success | `listen-surface-01-01` | secrets server-only; locked rows omit embed / BandLab link until unlocked |
| `listen-surface-01-04` | `done` | **IA:** command palette + **`/api/content/search`** include Listen hits; nav already had **`/listen`** | `listen-surface-01-02` | `pnpm run build` |
| `listen-surface-01-05` | `done` | **Route conventions** already list **`/listen`** (prior doc pass) | `listen-surface-01-04` | -- |

## Later (unscheduled)

| Id | Status | Note |
| --- | --- | --- |
| `listen-surface-02-*` | `idea` | optional **`/apps/listen`** only if Listen becomes primarily interactive tooling; v1 stays **`/listen`** per [route-and-urls](/docs/listen/route-and-urls) |

## Phase `listen-surface-02`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `listen-surface-02-01` | `done` | define how Listen uses the shared content-surface shell while keeping a distinct listening-room theme, mini logo, and media-first sidebar treatment | `listen-surface-01-05`, `documentation-site-09-01` | planning docs updated |
| `listen-surface-02-02` | `done` | apply the Listen identity contract to `/listen` without breaking the shared discovery primitives already used by blog and projects | `listen-surface-02-01` | `pnpm run build`, `pnpm run lint` |
| `listen-surface-02-03` | `done` | wire Listen into the shared type-to-search / command-palette behavior and animated icon treatment after the shared `documentation-site-09-03` / `09-04` pass | `listen-surface-02-02`, `documentation-site-09-03`, `documentation-site-09-04` | `pnpm run build`, manual listen search smoke |

## Phase `listen-access-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `listen-access-01-01` | `done` | define and start the DB-backed BandLab catalog model plus entitlement-gated visibility rules for private rows | `global-auth-01-01` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `listen-access-01-02` | `done` | replace the current shared-password unlock assumption with authenticated owner-session access for private rows while keeping public rows open | `listen-access-01-01`, `global-auth-01-02` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |

## Phase `listen-data-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `listen-data-01-01` | `done` | define the DB-seed/backfill contract so the file-authored catalog can upsert canonical Payload Listen rows without losing public/private visibility, slugs, dates, and tags | `listen-access-01-02` | planning docs updated |
| `listen-data-01-02` | `done` | add the actual seed/upsert path for real Listen rows, including owner-only private rows and repeatable local seeding | `listen-data-01-01` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `listen-data-01-03` | `done` | make `/listen`, home-page tracks, and Listen search prefer DB-backed rows while keeping the current file-authored catalog as fallback/bootstrap content | `listen-data-01-02` | `pnpm --filter @portfolio/app test:unit`, `pnpm run build`, `pnpm run lint` |
| `listen-data-01-04` | `done` | document and tighten the long-lived owner/admin workflow for maintaining Listen rows after the DB-backed source becomes primary | `listen-data-01-03` | docs updated, `pnpm run build` |
