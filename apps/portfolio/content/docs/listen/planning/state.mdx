---
title: State
description: Compact state records for the Listen section.
date: "2026-03-28"
updated: "2026-03-30"
order: 2
repoPath: apps/portfolio/content/docs/listen/planning/state.mdx
taskPhase: listen-access-01
---

# State

## Registry

| Field | Value |
| --- | --- |
| `section` | `listen` |
| `owner` | `listen-section` |
| `status` | `active` |
| `updated` | `2026-03-30` |

## Current cycle

| Field | Value |
| --- | --- |
| `phase` | `listen-surface-03` maintenance + `listen-access-01` |
| `plan` | the public listening-room surface is shipped; the first DB-backed catalog slice is now in place, and the next pass is to finish moving private owner-only tracks and presets off shared-password unlocks and onto the same authenticated session/entitlement model the reader uses |
| `focus` | keep `/listen` public, let DB-backed rows participate in the catalog now, and remove long-term shared-password assumptions for private media next |
| `constraint` | no public signup; private media should reuse the Global auth layer instead of inventing a Listen-only access scheme |

## Next queue

| Priority | Action | Type |
| --- | --- | --- |
| `1` | `listen-access-01-02`: migrate private access off shared-password unlock groups and onto authenticated owner sessions | `feature` |
| `2` | seed and backfill real DB-backed Listen rows once the owner-session gate replaces transitional unlock groups | `content` |
| `3` | continue catalog copy/data cleanup only when it supports the new backend-backed model | `content` |

## References

| Id | Path | Purpose |
| --- | --- | --- |
| `listen-page` | `apps/portfolio/app/listen/` | public Listen route |
| `music-lib` | `apps/portfolio/lib/music.ts` | current home-page track source |
| `catalog-lib` | `apps/portfolio/lib/listen-catalog.ts` | canonical listen catalog rows |
| `route-conventions` | `content/docs/documentation/route-conventions.mdx` | URL taxonomy |
| `payload-config` | `apps/portfolio/payload.config.ts` | shared auth and future media collections |
