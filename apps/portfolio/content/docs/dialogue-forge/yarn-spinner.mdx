---
title: "Yarn Spinner Format"
description: "How Dialogue Forge exports to Yarn Spinner for Unreal Engine"
date: "2024-12-18"
updated: "2026-04-04"
order: 3
---

# Yarn Spinner Format

Dialogue Forge exports to Yarn Spinner's `.yarn` format, which can be used directly in Unreal Engine with the Yarn Spinner plugin. Conversion code lives in `packages/dialogue-forge` (`exportToYarn` / `importFromYarn`).

## Basic Syntax

```yarn
title: Start
---
Stranger: You find yourself at a crossroads.
Stranger: What brings you here?
-> I seek treasure
    <<jump TreasurePath>>
-> I seek knowledge
    <<jump KnowledgePath>>
-> None of your business
    <<set $hostile = true>>
    <<jump HostilePath>>
===
```

## Node Structure

Each dialogue node becomes a Yarn node:

```yarn
title: NodeId
---
// Content goes here
===
```

## Variables (Memory Flags)

Set variables when choices are made:

```yarn
-> Accept the quest
    <<set $quest_accepted = true>>
    <<jump QuestStart>>
```

Check variables for conditional choices:

```yarn
<<if $has_key>>
-> Use the key to unlock the door
    <<jump UnlockedDoor>>
<<endif>>

<<if $gold >= 100>>
-> Bribe the guard (100 gold)
    <<set $gold -= 100>>
    <<jump BribedGuard>>
<<endif>>
```

## Visited Tracking

Yarn Spinner tracks visited nodes automatically:

```yarn
<<if visited("MetTheStranger")>>
Stranger: We meet again, traveler.
<<else>>
Stranger: A new face in these parts.
<<endif>>
```

## Export from the shipped app

1. Open **[Dialogue Forge](/apps/dialogue-forge)**.
2. Use **Graph** or **Yarn** mode (export is not wired for **Play** in the portfolio shell).
3. Trigger export from the editor: in **Yarn** mode, the Yarn view calls the app’s download handler; the graph view exposes export through the same `onExportYarn` wiring where the UI provides it.
4. Save the downloaded `.yarn` file and import it into Unreal with the Yarn Spinner plugin.

## Import a `.yarn` file

1. Switch to **Yarn** mode in the header.
2. In the Yarn view, use **Import .yarn** (file picker) to load text; the editor parses it and replaces the current dialogue tree via `importFromYarn`.

## Unreal Engine Integration

The Yarn Spinner Unreal plugin provides:

- Blueprint nodes for running dialogues
- Variable storage integration
- Custom commands for game logic
- Localization support

See [Yarn Spinner Unreal Docs](https://docs.yarnspinner.dev/using-yarnspinner-with-unreal) for setup.
