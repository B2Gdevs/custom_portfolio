---
title: State
description: Compact state records for the documentation section.
date: "2026-03-16"
updated: "2026-04-01"
repoPath: apps/portfolio/content/docs/documentation/planning/state.mdx
taskPhase: documentation-site-10
order: 2
---

# State

## Registry

| Field | Value |
| --- | --- |
| `section` | `documentation` |
| `owner` | `docs-section` |
| `status` | `active` |
| `updated` | `2026-03-31` |

## Current Cycle

| Field | Value |
| --- | --- |
| `phase` | `documentation-site-10` maintenance after `global-data-03` execution closed the public apps/projects/resumes metadata migration |
| `plan` | planning IA reset is shipped; the shared auth foundation, Books reader workspace polish, Books repub artifact merge, and the first Listen access/data phases are now in place. The cross-cutting storage baseline is real now: the heavy-blob push blocker is cleared, `/apps`, `/projects`, and `/resumes` all read the new record-backed metadata path while planning-pack exports and resume HTML routes continue to publish through `site-download-assets`. Listen and Reader runtime access now also share the same repository-backed provider pattern instead of scattering collection/query logic through route helpers. |
| `focus` | keep the global-first workflow aligned around the remaining shared data/storage provider work now that both the owner shell and the owner-only Payload admin mount are live, the public catalog metadata migration is done, and the Listen/Reader runtime contract is centralized behind provider-owned repositories |
| `constraint` | section planning MDX is first for **section** work; root `.planning` stays canonical for **monorepo** gates; see root `AGENTS.md` |

## Next Queue

| Priority | Action | Type |
| --- | --- | --- |
| `1` | extend the `site-download-assets` publisher to the next public bundle classes now that apps, projects, and resumes share the same metadata/storage split | `cross-cutting` |
| `2` | continue `global-data-01-03`: finish the local export-import and hosted bootstrap flow for the shared provider baseline | `cross-cutting` |
| `3` | **`global-data-01-04` done:** site RAG scope excludes default book-manuscript ingest; backend-matrix docs stay with **`global-data-01-03`** | `planning` |
| `4` | maintain Listen through real-row seeding and catalog cleanup now that the DB-first runtime path is in place | `content` |
| `7` | continue **`documentation-site-12`**: **`12-09`** auth + **Magicborn prompt composer** (**`global-auth-03`**, **`books-publishing-03`**); **`12-05`** SiteCopilot media session; **`12-03`** / **`12-04`** as before | `implementation` |

## Cross-cutting queue

Monorepo-wide items that are not (yet) a full `.planning` task row. Mirror or close them in **`.planning/TASK-REGISTRY.xml`** when they become single units of work.

| Status | Item |
| --- | --- |
| `done` | **`.planning-archive/`** removed (2026-03-28); migration pointers recorded in **[requirements](/docs/documentation/requirements)** (**Planning archive** section) and **`.planning/ROADMAP.xml`** / **`.planning/TASK-REGISTRY.xml`** phase 01 closure. |
| `done` | **Global auth foundation:** Payload auth baseline, shared entitlement/viewer helpers, host-facing login/logout/session routes, long-lived session policy, dev auto-login, and `pnpm --filter @portfolio/app auth:seed` are now in place. |
| `done` | EPUB planning supplement (`repub epub --planning`, `epubPlanningDirs`, `pnpm run build:books`); `.planning` task `01-04` + [books task registry](/docs/books/planning/task-registry). |
| `done` | Shared app logging layer (chat / RAG / structured debug). |
| `done` | Planning/operator docs now follow an ASCII-first punctuation policy; public-facing prose remains free to keep richer typography. |
| `open` | Blog + projects discovery pass -- blog/projects section registries. |
| `done` | **Listen** surface -- **`listen-surface-01`** implemented (`/listen`, catalog, Cmd+K); [listen task registry](/docs/listen/planning/task-registry). |
| `open` | **CLI ergonomics:** `documentation-site-07-01` / `07-02` -- Ink, **two CLIs**, submodule bumps, and the remaining publish / `pnpm dlx` docs polish per [requirements](/docs/documentation/requirements) CLI table. |
| `done` | **Reader backend pass:** auth-backed reader workspace, DB-backed reading-state sync, owner-only settings save, explicit local-EPUB upload, saved-upload shelf cards, record-aware deep links, and continue-reading defaults are now in place -- [books task registry](/docs/books/planning/task-registry). |
| `done` | **Listen private media pass:** private rows now use DB-backed visibility plus owner entitlements instead of shared-password unlock groups -- [listen task registry](/docs/listen/planning/task-registry). |
| `open` | **MCP / ChatGPT Apps (post–next-phases discussion):** owner requested a deep-dive after the current few phases — align [ChatGPT Apps / MCP Apps UX guidance](https://developers.openai.com/blog/what-makes-a-great-chatgpt-app) with reusing this repo’s Next.js reader and planning surfaces (portable tool-owned UI vs host-only chat chrome). No implementation commitment yet; capture requirements when scheduled. |
| `open` | **Dual backend data/storage phase:** the env-driven contract is now landed for Payload SQLite/Postgres plus local-disk/Supabase-S3 uploads, and Listen/Reader runtime access now goes through shared Payload-backed repositories instead of route-level collection/query logic. Supabase CLI linking, Codex MCP registration, the private `reader-library` bucket, and repo env templates are now in place. Remaining work is the final secret handoff, any further bootstrap automation, and the Books RAG boundary under `global-data-01`. |
| `open` | **Repo artifact and storage policy phase:** `repub-builder` is now a standalone vendored submodule/workspace package, the book-artifact publisher is live, the first public-media migration is live, and the heavy-blob push blocker is cleared. Current site imagery publishes to storage with a lightweight manifest bridge, and the app resolves those URLs without rewriting authored content. The remaining `global-data-02` work is extending those off-repo rules into more heavy public classes. |
| `done` | **Public catalog migration phase:** `global-data-03` is complete. `site-app-records`, `project-records`, `resume-records`, and `site-download-assets` now back the `/apps`, `/projects`, and `/resumes` metadata path, while planning-pack exports and resume HTML routes reuse the same published-asset bridge with repo fallbacks where planned. |
| `done` | **Owner-only shell refresh:** `global-auth-02` now reuses the existing seeded owner account and auth/session routes for the sidebar account footer, avatar, live identity/org context, logout, and owner-gated `/admin` entry points without opening public signup. |
| `done` | **Owner-only Payload admin mount:** `global-admin-01` now imports Payload's upstream `(payload)` route group, uses `/admin` as the owner-only CRUD surface for Payload-backed resources, and hides reader/RAG internals from the first mounted admin pass. |
| `done` | **Books build + site sidebar (2026-03-31):** EPUB repub cache logs **`CACHE HIT` / `CACHE MISS`**; epub-gen **`[Download Success]`** documented as local staging — [errors-and-attempts](/docs/documentation/planning/errors-and-attempts) `books-build-epub-log-01`; task **`documentation-site-10-05`**. Portfolio sidebar: hover expands rail until collapse; collapse-only control when expanded; icon rail buttons centered. |
| `open` | **Reader + artifacts hardening:** `/api/reader/workspace` worker must not import `server-only` (tsx child process). EPUB URLs in `public/books/manifest.json` hit Payload `/api/published-book-artifacts/file/…` — Payload errors + reader fetch failures until files exist under **`apps/portfolio/media/published-book-artifacts`** (run **`pnpm books:publish`** after **`pnpm run build:books`**, or restore media from backup). **`site-media-assets`** project/blog images 400 via `/_next/image` when rows point at missing files under **`apps/portfolio/media/site-media-assets`** — run **`pnpm media:publish`** or re-seed. Optional follow-up: reader fallback to **`public/books/<slug>/book.epub`** when the artifact file is missing but the public build exists. |
| `done` | **Local auth/session hang (2026-04-01):** `/api/auth/session` used a subprocess with **no timeout**; a stuck Payload/DB left the sidebar on **Checking session** and owner-gated pages (e.g. **`/resumes`**) on **HeadersTimeout**. Mitigation: **`AUTH_WORKER_TIMEOUT_MS`**, **`runAuthWorker` kills** the child on expiry, session route returns **anonymous session** on worker error, login returns **503** + message on timeout, client fetches use **AbortSignal.timeout**, **`pnpm start` + LAN host** can show Site Copilot shell without **`OPENAI_API_KEY`** (see **`app/layout.tsx`** / **`NEXT_PUBLIC_SITE_CHAT_HIDE_ON_LAN`**). Login over **http://localhost** is not encrypted — use **HTTPS** in production; JSON body visibility in DevTools is expected. |
| `open` | **Shared content surfaces:** align `listen`, `blog`, and `projects` on one reusable shell with per-section mini logos and theme identity -- `documentation-site-09`, section registries below. |
| `open` | **Minimal chrome for `/apps` tools:** tighten editor surfaces (screenshot-annotate, Dialogue Forge, repo-planner, etc.) — lean top bar + exit to `/apps`, avoid full marketing footer inside tools; see [screenshot-annotate decisions](/docs/screenshot-annotate/planning/decisions) and [requirements](/docs/screenshot-annotate/requirements). |
| `done` | **Planning IA reset:** literal section `planning/` folders are live, section docs/links now point at those paths, and one-off plans belong under `planning/plans/` instead of top-level implementation-plan drift -- `documentation-site-10`. |

## References

| Id | Path | Purpose |
| --- | --- | --- |
| `docs-root` | `apps/portfolio/content/docs/` | authored documentation source |
| `docs-page` | `apps/portfolio/app/docs/page.tsx` | documentation landing page |
| `docs-layout` | `apps/portfolio/components/docs/DocsLayout.tsx` | docs navigation and section layout |
| `docs-lib` | `apps/portfolio/lib/docs.ts` | docs grouping and planning-doc detection |
| `route-conventions` | `apps/portfolio/content/docs/documentation/route-conventions.mdx` | `/docs` vs `/apps` taxonomy and migration checklist |
| `reader-repo-planner-embed` | `apps/portfolio/content/docs/documentation/reader-repo-planner-embed-architecture.mdx` | repub-builder shell + repo-planner cockpit + portfolio host wiring |
| `docs-roadmap` | `apps/portfolio/content/docs/documentation/roadmap.mdx` | section-level overview of status and next work |
| `agents-root` | `AGENTS.md` (repo root) | Ralph loop + mandatory read order for agents |
| `planning-reqs` | [requirements.mdx](/docs/documentation/requirements) (this section) | monorepo requirements + planning-pack rules |
| `site-theme` | `apps/portfolio/app/globals.css`, `apps/portfolio/app/layout.tsx` | shadcn oklch tokens, `@theme inline`, IBM Plex, `dark` root class |
