---
title: Task Registry
description: Compact task records for the documentation section.
date: "2026-03-16"
updated: "2026-03-31"
taskPhase: documentation-site-10
repoPath: apps/portfolio/content/docs/documentation/planning/task-registry.mdx
order: 3
---

# Task Registry

## Phase `documentation-site-01`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-01-01` | `done` | create a section-local planning loop for documentation-site work | `-` | `pnpm exec next build` |
| `documentation-site-01-02` | `done` | capture the documentation site's overall requirements inside the docs tree | `documentation-site-01-01` | `pnpm exec next build` |
| `documentation-site-01-03` | `planned` | improve docs section navigation and planning-doc discoverability | `documentation-site-01-01` | `pnpm exec next build` |

## Phase `documentation-site-02`

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-02-01` | `done` | add a roadmap page with per-section status and next steps tied back to planning docs | `documentation-site-01-02` | `pnpm exec next build` |
| `documentation-site-02-02` | `done` | keep documentation requirements in a stable reference doc instead of scattering them through status records | `documentation-site-01-02` | `pnpm exec next build` |
| `documentation-site-02-03` | `planned` | expose the repo-level product-boundary and architecture-conventions planning docs from the documentation section without duplicating their source of truth | `documentation-site-02-02` | `pnpm exec next build` |

## Phase `documentation-site-03` (planning portal & agent hierarchy)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-03-01` | `done` | align root `AGENTS.md`, `.planning` docs, and section planning MDX with shared read order; document `repoPath` / `taskPhase` on key planning pages | `documentation-site-02-02` | `pnpm run build` |
| `documentation-site-03-02` | `done` | add `public/planning-pack/demo/*.md` template set + generic planning-agent `AGENTS.md` (example-only) | `documentation-site-03-01` | files present; `pnpm run build` |
| `documentation-site-03-03` | `done` | build script: section planning MDX -> `public/planning-pack/site/*.md` + `manifest.json` (excludes `.planning`) | `documentation-site-03-01` | `pnpm run build` |
| `documentation-site-03-04` | `done` | modal registry + Zustand; planning pack modal (tabs, thumbnails, hover actions, expand reader) | `documentation-site-03-02`, `documentation-site-03-03` | manual UX pass |
| `documentation-site-03-05` | `done` | gallery shows only existing files; no UI for missing per-section `AGENTS.md` | `documentation-site-03-04` | n/a |

## Phase `documentation-site-04` (section onboarding)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-04-01` | `planned` | add `magicborn` as a first-class docs section with the same planning-doc stack used by other active sections | `documentation-site-02-02` | docs content present |
| `documentation-site-04-02` | `planned` | keep documentation roadmap and requirements aligned as `magicborn` becomes part of the public worldbuilding and RAG source map | `documentation-site-04-01` | planning docs updated |
| `documentation-site-04-03` | `planned` | onboard `projects` as a first-class planning section so public project UX and schema work can be tracked beside blog and books instead of in ad hoc notes | `documentation-site-02-02` | docs content present |

## Phase `documentation-site-05` (shadcn site theming)

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-05-01` | `done` | record decisions: single token source in `globals.css` (`:root` / `.dark`), IBM Plex stacks, legacy Tailwind aliases (`text-text`, `bg-dark`, `text-accent-*` chart mapping) | `-` | this registry + `decisions.mdx` |
| `documentation-site-05-02` | `done` | apply theme: `layout.tsx` (`dark` + `next/font` IBM Plex Sans/Serif/Mono), merge user oklch palette + `@theme inline` + `@layer base`, keep green prose link palette | `documentation-site-05-01` | `pnpm run build` (no dev server lock on `.next`) |
| `documentation-site-05-03` | `done` | align prose / scrollbars / EPUB reader CSS variables with semantic tokens; fix invalid utilities (e.g. `hover:bg-text-primary`) | `documentation-site-05-02` | visual smoke on docs + reader |

## Phase `documentation-site-06` (URL taxonomy + `/apps` hub)

Aligned with [Route conventions](/docs/documentation/route-conventions). **Do not** break `/apps/reader` while migrating.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-06-01` | `done` | publish **Route conventions** MDX + registry/decision links (this phase) | `-` | `/docs/documentation/route-conventions` renders |
| `documentation-site-06-02` | `done` | move **Apps hub** from `app/docs/apps/page.tsx` to **`app/apps/page.tsx`**; **`next.config`** redirects: `/docs/apps` -> `/apps`, `/docs/apps/:path*` -> `/apps/:path*`; extend `/docs/tools` redirect to `/apps` | `documentation-site-06-01` | `pnpm run build`, manual hit old URLs |
| `documentation-site-06-03` | `done` | move **`repo-planner`** page to `app/apps/repo-planner/page.tsx`; update **Nav**, **DocsFileTreeNav**, **RepoPlannerModal**, **RepoPlannerCockpitClient**, and MDX links (global, repo-planner, vendoring) | `documentation-site-06-02` | `pnpm run build`, grep for stale `/docs/apps` |
| `documentation-site-06-04` | `planned` | optional: add **lint or CI grep** guard for forbidden pattern `docs/apps` in source (allow-list MDX history if needed) | `documentation-site-06-03` | grep / review |
| `documentation-site-06-05` | `done` | **Dialogue Forge** -> **`/apps/dialogue-forge`**; **308** from `/dialogue-forge`; update project MDX, tests, apps hub link; **remove `/books` pages** + **redirects** per [Route conventions](/docs/documentation/route-conventions) | `documentation-site-06-01` | `pnpm run build` |
| `documentation-site-06-06` | `done` | finish the route/navigation sweep after the `/apps` move: add an explicit **Docs** entry to the main site side nav, stop treating apps as docs children in the docs file tree, and update homepage/sidebar affordances so docs and apps are both first-class destinations | `documentation-site-06-02`, `documentation-site-06-03` | `pnpm run build`, manual nav sweep |

## Phase `documentation-site-07` (monorepo CLI ergonomics)

**Goal:** first-class **terminal UX** for **two separate CLIs** -- **RepoPlanner** (`loop-cli`, submodule **`vendor/repo-planner`**) and **`@portfolio/repub-builder`** (`repub`). **Decisions** (see [requirements](/docs/documentation/requirements) CLI table): **Ink** for rich TTY; **breaking argv** allowed; **document `pnpm dlx` / `npx`-style usage** for v1 (npm publish later); **`engines.node` >= 22** when publishing; **Next.js-only** story for published **RepoPlanner UI** embed; implementation lands in **submodule** for planner CLI.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-07-01` | `planned` | **RepoPlanner:** upstream **`loop-cli.mjs`** + help/version/subcommands + **Ink** where appropriate; bump **submodule**; root **`pnpm planning`** stays a thin wrapper with **`REPOPLANNER_REPORTS_DIR`**; document **`pnpm dlx`** / invocation from repo | `documentation-site-06-01` | `pnpm planning -- --help`; docs in [Repo Planner getting started](/docs/repo-planner/getting-started) |
| `documentation-site-07-02` | `planned` | **repub-builder:** `repub --help`, subcommand help (`build` / `read` / `epub`), **Ink** TTY UX; **`engines.node` >= 22**; **`package.json` `bin`** + README **`pnpm dlx`** recipes; **publish to npm later** (remove `private` when ready) | `-` | `pnpm exec repub --help` from workspace |

## Phase `documentation-site-08` (reader shell + cockpit embed + docs context)

**Goal:** **One** embeddable **Repo Planner cockpit modal body**; **portfolio** `ModalRoot` + **repub-builder** **reader modal store** both open it; **docs** file-tree **context menu** on planning folders opens cockpit with an agreed payload. See [Reader shell + Repo Planner embed architecture](/docs/documentation/reader-repo-planner-embed-architecture).

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-08-01` | `done` | publish architecture MDX + Mermaid; link from [repo-planner planning-docs](/docs/repo-planner/planning/planning-docs) + documentation planning-docs | `-` | page renders |
| `documentation-site-08-02` | `planned` | **docs FileTree:** context action "Planning cockpit" / "Open in cockpit" on folders with `folderArchivePrefix` or section planning roots; `openModal(REPO_PLANNER_MODAL_ID, payload)` | `documentation-site-08-01`, `books-reader-03-06` | manual smoke |
| `documentation-site-08-03` | `planned` | **portfolio:** refactor `RepoPlannerModal` to consume **shared** body export from **repub-builder** (or agreed facade); remove duplicate layout | `books-reader-03-06` | `pnpm run build` |

## Phase `documentation-site-09` (shared content surfaces)

**Goal:** unify `listen`, `blog`, and `projects` around one discovery/detail component system while preserving clear section identity through theme tokens, mini logos, and sidebar marks.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-09-01` | `planned` | define the shared content-surface contract: reusable index/detail primitives, identity token slots, section mini-logo hooks, and what stays section-specific for listen/blog/projects | `documentation-site-02-02` | planning docs aligned across documentation, listen, blog, and projects |
| `documentation-site-09-02` | `planned` | track approved external shadcn registries for future UI work: `@lucide-animated` for animated icons and `@magicui` for effect-oriented components; document install commands and intended usage boundaries before adoption | `documentation-site-09-01` | vendoring doc updated; no accidental package install |
| `documentation-site-09-03` | `done` | implement section identity on the shared content surfaces: blog, projects, and listen each get their own theme tokens, mini logo, and animated icon treatment without forking the shared discovery/detail shell | `documentation-site-09-01`, `documentation-site-09-02`, `blog-identity-01-01`, `projects-identity-01-01`, `listen-surface-02-01` | `pnpm run build`, `pnpm run lint` |
| `documentation-site-09-04` | `done` | harden shared content search behavior: command palette should open when type-to-search starts, debounce live queries, and stop the multi-character client exception before identity polish continues | `documentation-site-09-01` | `pnpm --filter @portfolio/app test:unit`, manual `cmd+k` / type-open smoke |

## Phase `documentation-site-11` (resume gallery surface)

**Goal:** turn `/resumes` into a visual gallery rather than a text card list, while keeping the HTML files as the source of truth.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-11-01` | `done` | redesign `/resumes` as a gallery of HTML preview thumbnails/cards with hover actions surfaced as icon buttons + tooltips (`open`, `print`, `download`, future PDF if added) instead of stacked CTAs below each card | `documentation-site-02-02` | `pnpm run build`, manual hover/action smoke |

## Phase `documentation-site-10` (planning IA and foldering)

**Goal:** make the planning loop roadmap-first, stop treating implementation plans as top-level living records, and migrate section planning docs into literal `planning/` folders that also support pack rendering.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-10-01` | `done` | define the target planning-file layout for sections: `planning/` folder, living records, optional `planning/plans/<phase-id>/` for one-off phase plans, and what remains as non-planning reference docs at section root | `documentation-site-02-02` | global planning + documentation docs aligned |
| `documentation-site-10-02` | `done` | update agent guidance and docs guidance so roadmap/state/task-registry/decisions are the living loop and `IMPLEMENTATION_PLAN` becomes a legacy compatibility pointer instead of the behavioral default | `documentation-site-10-01` | root AGENTS + repo-planner docs + `.planning` guidance aligned |
| `documentation-site-10-03` | `done` | migrate section planning docs into literal `planning/` folders without breaking site routes, packs, or docs navigation; leave redirects/aliases or route mapping notes where needed during transition | `documentation-site-10-01` | `pnpm run build`, docs paths verified |
| `documentation-site-10-04` | `done` | add section-local `requirements.mdx` and `planning/roadmap.mdx` records for active docs sections, and rewrite read-order guidance so `planning-docs.mdx` is a playbook rather than the canonical planning record | `documentation-site-10-03` | `pnpm run build`, `pnpm run lint` |

## Phase `documentation-site-12` (apps hub cards, compact planning-pack control, media copilot)

**Goal:** Stripe-style integrated docs/apps UX: a **reusable app card** (cover image, actions, tooltips), a **compact toolbar-style planning-pack control** (not a hero tile), and a **focused media workflow** (generate / iterate / pick cover) launched from a **magic** control on **cover hover** that opens **SiteCopilot** with **`mediaSlot`** context (**no manual media-mode toggle**). **OpenAI Images** for generation; API route separate from RAG `/api/chat`. Session-scoped image memory + later Payload/S3/Supabase persistence. Thread layout informed by assistant-ui **shadcn** example ([`shadcn.tsx` thread + composer + parts](https://github.com/assistant-ui/assistant-ui/blob/main/apps/docs/components/examples/shadcn.tsx)) — gallery + markdown + image parts in the transcript.

**Decisions:** `DOCUMENTATION-SITE-MEDIA-IMAGE-OPENAI`, `DOCUMENTATION-SITE-MEDIA-COPILOT-CONTEXT`. Remaining: auth for generate/persist, slot history policy.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-12-01` | `done` | **Kickoff:** `planning/plans/documentation-site-12/` — provider + copilot-shell decisions recorded; slot/API boundaries documented; **`.cursor/mcp.json`** adds [shadcn MCP](https://ui.shadcn.com/docs/mcp) + Supabase MCP (`project_ref` as provided) | `documentation-site-09-01` | decisions + kickoff + MCP file in repo |
| `documentation-site-12-02` | `done` | **`AppsHubAppCard`**: `/apps` hub cards; icon/cover tile with **hover/focus magic** (wand) → **`openChatForMediaSlot(`**`app-cover:{id}`**`)**`; no mode toggle | `documentation-site-12-01` | manual `/apps`; `SiteCopilot` slot-aware copy |
| `documentation-site-12-03` | `planned` | **`PlanningPackToolbarButton`**: compact icon-first control for toolbars/article chrome; optional **animated** icon via approved registry ([`documentation-site-09-02`](/docs/documentation/planning/task-registry#phase-documentation-site-09-shared-content-surfaces)); demote/remove oversized “featured” planning CTA from article surfaces | `documentation-site-12-01`, `documentation-site-09-02` | visual + a11y pass |
| `documentation-site-12-04` | `planned` | **Client session store** for generated images: in-memory + `sessionStorage` (and optional `localStorage` policy); list UI for “this session” gallery; no data loss on refresh within session | `documentation-site-12-01` | unit tests for hydrate/merge |
| `documentation-site-12-05` | `planned` | **Extend `SiteCopilot`:** when `mediaSlot` is set from card, show **media session UI** (gallery rail + image-capable thread + composer); clearing slot returns to default RAG chat; **Set as cover** commits URL into slot registry | `documentation-site-12-02`, `documentation-site-12-04`, `books-ai-01-07` | manual E2E happy path |
| `documentation-site-12-06` | `done` | **`POST /api/media/generate`**: OpenAI Images (`OPENAI_IMAGE_MODEL`, default `dall-e-3`), **`b64_json`**, optional **`mediaSlot`**; structured errors; **`OPENAI_IMAGE_E2E`** opt-in Playwright quota test | `documentation-site-12-01` | `tests/unit/api/media-generate-route.test.ts`; `tests/e2e/media-generate.spec.ts` |
| `documentation-site-12-07` | `planned` | **Payload + S3 (phase 2):** persist accepted images, link to `appId` + variant; admin/owner upload path; public read rules | `global-data-01` closure or scoped spike | integration test |
| `documentation-site-12-08` | `planned` | **Batch covers:** script or owner UI — iterate registered apps, run templated prompt, write artifacts to agreed path or collection | `documentation-site-12-06`, `documentation-site-12-07` optional | one full run documented |

## Phase `documentation-site-13` (RAG cost: local embeddings)

**Goal:** keep **chat completions / images** on OpenAI where needed, but **drop OpenAI embedding spend** for RAG when desired by using **CPU MiniLM** and a **gitignored** cache.

**Decision:** `DOCUMENTATION-RAG-EMBEDDINGS-LOCAL-MINILM`. See **`apps/portfolio/models/README.md`**.

| Id | Status | Goal | Depends | Verify |
| --- | --- | --- | --- | --- |
| `documentation-site-13-01` | `done` | **`RAG_EMBEDDING_PROVIDER=local`** → **`embedTexts`** uses **`@xenova/transformers`** + default **`Xenova/all-MiniLM-L6-v2`** (384d); cache dir under **`models/.transformers-cache/`** | `-` | `tests/unit/lib/rag/embedding-config.test.ts`; manual delete DB + re-ingest when switching |
| `documentation-site-13-02` | `planned` | **Operator doc** in [requirements](/docs/documentation/requirements) or global planning: when to use local vs OpenAI embeddings, quota pitfalls, CI recipe | `documentation-site-13-01` | docs PR + link from `models/README.md` |
