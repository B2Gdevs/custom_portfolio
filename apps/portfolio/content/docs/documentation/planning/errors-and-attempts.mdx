---
title: Errors and Attempts
description: Compact attempt records for the documentation section.
date: "2026-03-16"
updated: "2026-03-29"
order: 4
---

# Errors and Attempts

| Id | Status | Context | Problem | Response |
| --- | --- | --- | --- | --- |
| `documentation-site-01-attempt-01` | `resolved` | section planning setup | the docs site itself had no local planning section even though other active sections did | add compact planning-doc pages under `docs/documentation` |
| `documentation-site-02-attempt-01` | `resolved` | docs status visibility | the docs tree had no single overview showing where each section stands and what is next | add a roadmap page under the documentation section and keep it aligned with section planning records |
| `documentation-site-10-attempt-01` | `resolved` | planning IA reset | section planning docs lived at section root, old links still pointed at flat routes, and the root compatibility docs still nudged agents toward legacy paths | move planning records into literal `planning/` folders, sweep stale links/paths, and verify with `pnpm run build` plus `pnpm run lint` |
| `documentation-site-cmdk-listen-01` | `resolved` | Cmd+K / global content search | operators typing **`/listen`** (often after `/` then more keys) saw empty or unstable command-palette results; listen rows were not indexed on **`href`**, and scoring/highlight paths assumed every optional field was present | add **`href`** + **`slug`** to **`buildSearchBlob`**, score **`href`/`slug`**, null-safe **`scoreDiscoveryItem`** / **`getSnippetSource`** / **`filter`**, **`try/catch`** around highlight **`RegExp`**, safe group labels in **`ContentCommandPaletteModal`**; regression tests in **`tests/unit/lib/content-discovery.test.ts`** |
