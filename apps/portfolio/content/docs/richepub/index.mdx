---
title: RichEPub
description: Documentation for the RichEPub (.repub) format—React, Tailwind, and npm in a single ebook container.
---

# RichEPub

RichEPub is a custom ebook container format (**.repub**) that supports React, Tailwind CSS, and npm dependencies. It is intended for web-based readers only and is not compatible with standard EPUB readers like Kindle or Apple Books.

## Documentation

- **[Format Specification](./format-spec)** – Container layout, `repub.json` schema, dependency semantics, and versioning.

## Related

- **[Book reader (static EPUB & RichEPub)](/docs/books/reader)** – Embed the reader in docs; it reads from static `book.epub` and `book.repub` files.
- Standard **EPUB** is used for universal reading; see the books and export tooling for generating .epub from book content.
- The portfolio includes a **.repub viewer** that unpacks and runs .repub files in a sandboxed iframe.
