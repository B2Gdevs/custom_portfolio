name: Sync Packages to GitHub Repos

on:
  push:
    branches: [main]
    paths:
      - 'packages/**'
  workflow_dispatch:

jobs:
  sync-packages:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package:
          - dialogue-forge
        # Add more packages here as you create them
    
    steps:
      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Sync ${{ matrix.package }} to GitHub repo
        working-directory: packages/${{ matrix.package }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Initialize git if needed
          if [ ! -d ".git" ]; then
            git init
            git branch -M main
            git remote add origin https://github.com/MagicbornStudios/${{ matrix.package }}.git || true
          fi
          
          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Update remote URL with token for authentication
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/MagicbornStudios/${{ matrix.package }}.git
          
          # Ensure we're on main branch
          git checkout -b main 2>/dev/null || git checkout main
          
          # Add all files
          git add -A
          
          # Commit if there are changes
          git commit -m "Sync from monorepo main branch [skip ci]" || echo "No changes to commit"
          
          # Push to package repo (this will trigger publish workflow)
          git push -u origin main || git push -u origin main --force
          
          echo "✅ Synced ${{ matrix.package }} to GitHub"
          echo "📦 Package repo will automatically publish to npm"

