# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies (root and all workspaces)
node_modules/
.pnp
.pnp.js
.pnp.cjs
.yarn/install-state.gz
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml

# testing
/coverage
apps/portfolio/coverage/
apps/portfolio/test-reports/**
!apps/portfolio/test-reports/README.md
playwright-report/
test-results/
**/playwright-report/
**/test-results/

# next.js (anywhere in repo; do not commit build/cache)
.next/
**/.next/
# Isolated production build for `magicborn chat --serve` / `pnpm chat:build` (PORTFOLIO_DIST_DIR)
apps/portfolio/.next-chat/
out/
**/out/

# production / build output
build/
**/dist/

# Magicborn CLI local runs (generated images + manifests)
.magicborn/

# book build cache (scripts/build-books.cjs)
.tmp/

# misc
.DS_Store
*.pem
nul
.playwright-mcp/

# GSD Codex install (local skills; avoid Windows nul git error)
.codex/
.claude/
.agents/
.cursor/
.trae/
skills-lock.json

# npx skills add --agent * (project-local copies into many tool dirs; not committed)
.adal/
.augment/
.bob/
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.vibe/
.windsurf/
.zencoder/

# debug
.logs/*
!.logs/README.md
apps/portfolio/.logs/
# Codex / local CLI captures next to the app package
apps/portfolio/.codex*.log
# Shell/IDE redirects of `next dev` / `next start` (not Next's `.next/` dir, not app JSONL logs)
.next-dev-*.log
.next-start-*.log
.tmp-next*.log
.tmp-grimetime-start*.log
chat-runtime-env.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# database
*.db
*.db-shm
*.db-wal
*.db-journal
/drizzle
apps/portfolio/public/books/**/book.epub
apps/portfolio/public/books/**/checkpoints/**
apps/portfolio/public/books/**/*.repub

# Release artifacts (downloaded by scripts/download-releases.cjs)
.releases/

# Local frozen reader builds
.standalone/
.reader-builds/
**/.reader-builds/

# RepoPlanner CLI telemetry + generated reports (local only; do not commit)
.planning-reports/
.planning/reports/

# GAD CLI call logs (local telemetry — not committed, but eval logs are copied into eval runs)
**/.gad-log/

# Local ML weights / transformers.js cache (MiniLM etc.); keep apps/portfolio/models/README.md tracked
apps/portfolio/models/.transformers-cache/
apps/portfolio/models/**/*.onnx
apps/portfolio/models/**/*.bin
.tmp/
